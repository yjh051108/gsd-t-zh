| Date | Command | Step | Model | Duration(s) | Severity | Finding |
|------|---------|------|-------|-------------|----------|---------|
| 2026-04-17 | red-team | M39 W1 | opus | 1200 | CRITICAL | GSD_T_AGENT_ID has no producer — 189 shims write nothing at runtime (D2 feature dead-on-arrival). See .gsd-t/red-team-report.md BUG-1 |
| 2026-04-17 | red-team | M39 W1 | opus | 1200 | CRITICAL | --worker-timeout CLI flag referenced by contract §16 is not parsed by parseArgs; config.workerTimeoutMs also not merged into opts. No escape hatch for >270s iters. BUG-2 |
| 2026-04-17 | red-team | M39 W1 | opus | 1200 | HIGH | bin/gsd-t-unattended.js diverged from .cjs — M39 D2/D3/D4 changes landed only in .cjs; .js still has 3600000 default, no Team Mode, no watch-progress banner. BUG-3 |
| 2026-04-17 | red-team | M39 W1 | opus | 1200 | HIGH | Team Mode prompt + contract §15 reference "Step 4 Team Mode" but Team Mode is Step 3 in commands/gsd-t-execute.md. BUG-4 |
| 2026-04-17 | red-team | M39 W1 | opus | 1200 | MEDIUM | commands/gsd-t-execute.md Step 5.25 shim has truncated --step-label ending "design contract exist" (missing "s)"). BUG-5 |
| 2026-04-17 | red-team | M39 W1 | opus | 1200 | MEDIUM | buildTree with cyclic parent_agent_id refs produces empty tree silently (nodes attach to each other's children, never reach roots/orphans). BUG-6 |
| 2026-04-17 | red-team | M39 W1 | opus | 1200 | LOW | Contract §18 references bin/gsd-t-unattended.js but M39 code lives in .cjs. BUG-7 |
| 2026-04-17 | red-team | M39 W1 | opus | 1200 | LOW | Contract §13 config.json example still shows workerTimeoutMs: 3600000 (old default). BUG-8 |
| 2026-05-06 09:03 | gsd-t-plan | Step 7 | haiku | 37 | low | Validator initially flagged REQ traceability (Check 1) as FAIL — root cause: git stash/pop dance during baseline test capture dropped the in-progress append to docs/requirements.md. Re-appended (11 rows REQ-M47-D1-01..07 + REQ-M47-D2-01..04). Re-verified by direct grep. All other 6 checks PASS. |
| 2026-05-06 11:00 | red-team | M48 | opus | 600 | MEDIUM | `$&` (and other `$`-substitution patterns) in project basename corrupt the rendered title. `String.prototype.replace(/__PROJECT_NAME__/g, _escapeHtml(name))` interprets `$&` in the replacement string as the matched text, re-injecting the placeholder. Project at `/path/name$&literal` renders `<title>name__PROJECT_NAME__amp;literal</title>` instead of `<title>name$&amp;literal</title>`. Fix: use function-form replacement `() => _escapeHtml(projectName)`. See `.gsd-t/red-team-report.md` BUG-1. |
| 2026-05-06 11:00 | red-team | M48 | opus | 600 | LOW | Legacy `renderTree()` click handler at `gsd-t-transcript.html:872-875` does not guard against in-session entries; siblings of `mainEntry` (older in-session NDJSONs) land in the live bucket and rendering uses the unguarded handler. Click mutates URL hash but `hashchange` early-return prevents bottom-pane pinning. Cosmetic state pollution only. Copy the `if (isInSession) return;` guard from `renderRailEntry`. See BUG-2. |
| 2026-05-06 11:00 | red-team | M48 | opus | 600 | LOW | Test quality — 14 of 19 M48 tests assert against raw HTML source strings (e.g. `assert.match(html, /new Date\(frame\.ts\)/)`) instead of executing the function and asserting on observable output. A buggy implementation that contained the right tokens but had wrong behaviour would pass. Violates CLAUDE.md § E2E Test Quality Standard. Functional rewrites recommended for Bug 2/3/4 tests. See BUG-3. |

| 2026-05-06 14:01 | gsd-t-execute | Step 5.5 (Red Team) | opus | 302s | HIGH | BUG-1 — `pubspec.yaml`/`tailwind.config.{js,ts}` as directories yielded false positives. FIXED in same cycle (statSync().isFile()). |
| 2026-05-06 14:01 | gsd-t-execute | Step 5.5 (Red Team) | opus | 302s | HIGH | BUG-2 — dot-dir over-exclusion (impl skipped all `.X/` dirs; contract §4 enumerates 8). FIXED in same cycle (rely on IGNORED_DIRS only). |
| 2026-05-06 14:01 | gsd-t-execute | Step 5.5 (Red Team) | opus | 302s | MEDIUM | BUG-3 — `tailwind.config.mjs` not detected. FIXED (added .mjs/.cjs). |
| 2026-05-06 14:01 | gsd-t-execute | Step 5.5 (Red Team) | opus | 302s | MEDIUM | BUG-4 — Astro projects undetected. DEFERRED — contract §4 amendment required; logged to deferred-items. |
| 2026-05-06 14:01 | gsd-t-execute | Step 5.5 (Red Team) | opus | 302s | MEDIUM | BUG-5 — Nuxt deps undetected (only `.nuxt/` build dir is). DEFERRED — contract §4 amendment required; logged to deferred-items. |
| 2026-05-18 | gsd-t-qa | M57 D1 | sonnet | — | MEDIUM | SHALLOW TEST — `checkBuildCoverage return shape has all required fields` uses `_newPaths:[]` (empty diff early-return path) to test field presence. `checkedAgainst` will always be `[]` in this path; the test never exercises the shape when CI artifacts ARE detected. Would pass on a stub that returns `{ok:true,missing:[],checkedAgainst:[],newPaths:[]}` for empty diff. |
| 2026-05-18 | gsd-t-qa | M57 D2 | sonnet | — | MEDIUM | SHALLOW TEST — `docker_unavailable_is_not_hard_failure` test is named for the `docker-unavailable` contract path but tests `no-dockerfile` instead. The test body explicitly acknowledges it cannot simulate the missing-docker-binary path and falls back to asserting `dockerSkippedReason === 'no-dockerfile'` on a fixture that has no Dockerfile. The actual `docker-unavailable` behavior (Dockerfile present + docker binary absent → `ok:true`, `dockerSkippedReason:'docker-unavailable'`) is never verified. Would pass even if the `docker-unavailable` branch set `ok:false`. |
| 2026-05-18 | gsd-t-qa | M57 D2 | sonnet | — | LOW | SHALLOW TEST — `cli_json_output_has_correct_shape` only checks key existence (`'ok' in out`, `'detectedSource' in out`, etc.), not field values. A broken implementation that returned `{ok:null,detectedSource:'',commands:null,dockerBuilt:null}` would pass. Should assert `typeof out.ok === 'boolean'`, `out.detectedSource === 'package-scripts'`, `Array.isArray(out.commands)`, `typeof out.dockerBuilt === 'boolean'`. |
| 2026-05-18 | gsd-t-qa | M57 D1 | sonnet | — | HIGH | COVERAGE GAP — No test for `COPY --from=` exclusion. Contract explicitly states "`COPY --from=` (multi-stage image copy) is excluded — those reference image layers, not workspace paths." The `docker-cloudbuild` fixture Dockerfile contains `COPY --from=builder /app/dist ./dist`. There is no test that verifies `dist` appears in `missing[]` when `dist/` is in the new paths diff (i.e., that `--from` exclusion is in effect and `dist` is NOT counted as covered). |
| 2026-05-18 | gsd-t-qa | M57 D2 | sonnet | — | MEDIUM | COVERAGE GAP — `docker-unavailable` path (Dockerfile present, docker binary absent → `ok:true`, `dockerSkippedReason:'docker-unavailable'`) has no functional test. Contract §Docker Trigger: "docker binary missing → `dockerBuilt:false`, `dockerSkippedReason:'docker-unavailable'`, NOT a hard failure." The only test claiming to cover this path actually tests `no-dockerfile` instead. Recommend: use PATH override (`{env:{PATH:'/nonexistent'}}` in spawnSync) to force docker-unavailable state. |

| 2026-05-18 | gsd-t-qa | M57 Fix Cycle 1 Verification | haiku | 5m | PASS | All 5 QA issues resolved with functional assertions. Unit test suite: 36/36 M57 tests PASS, 1 by-design SKIP (Docker-dependent SC2 full-build on Docker-less host). Issue 1: `checkBuildCoverage return shape: checkedAgainst non-empty when CI artifacts detected` (lines 309–330) — assert on docker-cloudbuild fixture with non-empty diff, verifies checkedAgainst.length>0 and includes 'Dockerfile' (fails on empty-diff stub). Issue 2: `COPY --from= exclusion: dist covered by --from= layer does NOT prevent missing[]` (lines 336–356) — inject dist+hooks, assert dist in missing[] (proves --from= excluded), ok:false. Issue 3: `docker_unavailable_is_not_hard_failure` (lines 184–214) — real Dockerfile+no-docker path (PATH override), assert ok:true, dockerBuilt:false, dockerSkippedReason:'docker-unavailable' (not skipped). Issue 4: `no_dockerfile_skips_docker_non_failure` (lines 176–182) — distinct test, no Dockerfile, assert dockerSkippedReason:'no-dockerfile'. Issue 5: `cli_json_output_has_correct_shape` (lines 342–356) — now asserts typeof/value (bool, string, Array, bool), not just key existence. All assertions functional (fail on broken stubs). |
