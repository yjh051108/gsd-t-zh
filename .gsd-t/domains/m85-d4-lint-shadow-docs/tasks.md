# Tasks: m85-d4-lint-shadow-docs

## Summary
Enforcement + measurement + doc ripple — fully write-disjoint from all M85 source files. Owns the NEW M71-family lint (read-only assertion over all 8 workflows + the policy module/contract), the `gsd-t-audit` shadow probe that records a MEASURED verdict in `progress.md`, and the full doc ripple. Never writes a workflow, the policy module, or a `bin/` consumer.

## Tasks

### M85-D4-T1 — M71-family tier-policy lint (the drift enforcer)
- **Touches**: `test/m85-workflow-tier-policy-lint.test.js` (NEW)
- **Files**: `test/m85-workflow-tier-policy-lint.test.js`
- **Test**: self — `node --test test/m85-workflow-tier-policy-lint.test.js`
- **Contract refs**: `.gsd-t/contracts/model-tier-policy-contract.md` v1.0.0 § "Drift Enforcement"
- **Deps**: BLOCKED by M85-D1-T1 (needs the policy module/contract to assert against)
- **Acceptance criteria**:
  - Reads all 8 `templates/workflows/*.workflow.js` (read-only) + the policy module/contract; asserts every workflow `model:` value (string literal OR the operand(s) of a ternary like `cycle===1 ? "opus" : "fable"`) is a member of the policy tier set `{opus, fable, sonnet, haiku}`. The 8 workflows carry varying counts (scan 15, phase 9, verify 5, debug/execute/integrate 2, quick 1, wave 0) — ALL must pass the membership check, not just the designated ones. *(Falsifier: a workflow literal outside the set passes the lint, or the ternary operands are not unwrapped. Test: self — positive membership assertion over every `model:` occurrence in all 8 files.)*
  - **Stage identification (pre-mortem r1 finding #2):** the lint maps a designated stage to its literal by keying off the `agent()` call's `label:` field — NOT by line number (line numbers drift). Non-designated literals (e.g. verify `synthesis`/`code-review-ultra`/`qa`, the haiku CLI helpers, scan's stages) are checked for MEMBERSHIP ONLY, never forced to fable. *(Falsifier: the lint keys off brittle line numbers, or forces a non-designated stage to fable. Test: self — assert each designated `label:` maps to the contract tier; assert a representative non-designated label is unconstrained beyond membership.)*
  - **Explicit stage-key → source-label mapping table, asserted NON-EMPTY per stage (pre-mortem r2 finding #3 — contract keys and live labels are DIFFERENT strings; a mapping that matches zero `agent()` calls makes "every member of {} is fable" vacuously true):** the lint carries an explicit, asserted mapping table reconciling contract STAGE_TIERS keys to live source-label shapes: `solution-space-probe → label "solution-space-probe"`, `partition-probe → label "partition-probe"`, `competition-judge → label "judge:rubric"`, `competition-producers → labels matching /^candidate:/` (template literal `candidate:${...}`), `pre-mortem → label "pre-mortem"`, `red-team → label "red-team"`, `debug-cycle-2 → the cycle===2 branch of the "debug-cycle-${cycle}" call's ternary`. For EVERY designated mapping the lint asserts ≥1 real `agent()` call matched in the live files — a designated stage whose mapping matches ZERO calls FAILS the lint loudly (fail-closed, extending the real-file discovery assertion from file-count to per-designated-stage match-count). If a label is renamed in a workflow, the lint breaks loudly instead of passing vacuously. *(Falsifier: the lint hard-codes the contract key `competition-judge` as a label, matches nothing, and the judge≠producer invariant passes on an empty set. Test: self — per-stage non-empty-match assertion + a meta-case where a mapping deliberately points at a nonexistent label and the checker reports a violation.)*
  - Asserts the 5 designated stages resolve to `fable` AND `competition-producers` resolves to `opus` (the held invariant); for `debug-cycle-${cycle}` the lint unwraps the per-cycle ternary and asserts cycle-1→opus, cycle-2→fable. *(Falsifier: a designated stage maps to non-fable, producers to non-opus, or the debug ternary is mis-read, and the lint passes. Test: self — per-stage assertion incl. debug conditional.)*
  - **Mandatory negative test**: a deliberately-drifted literal (e.g. an in-test fixture reverting Red Team to `opus`, setting a stage to `sonnet`, or flattening the debug ternary so cycle-1 becomes fable) MUST make the lint's checker FUNCTION return a violation — asserted via a returned-violations assertion / `assert.throws` against a synthetic drifted-source fixture (NOT by failing the live suite). *(Falsifier per AC a: the drift fixture passes the checker → the lint is decorative. Test: self — negative-case assertion is REQUIRED.)*
  - **Real-file discovery assertion (pre-mortem finding #5 — the lint must provably read the LIVE files):** the lint asserts its file-discovery resolved EXACTLY 8 `*.workflow.js` files from `templates/workflows/` AND that each of the 7 non-wave workflows yielded ≥1 `model:` occurrence (wave has 0 agent calls). A glob matching zero files, or a label-regex that silently no-ops on the real `agent()` call shape, MUST fail loudly. *(Falsifier: the checker rejects fixtures but silently scans nothing real — decorative one level up. Test: self — discovery-count + per-file occurrence-count assertions.)*
  - **Meta-test through the real-file code path (pre-mortem r1 finding #5):** copy ONE real workflow file (e.g. `gsd-t-verify.workflow.js`) to a temp location, drift exactly one designated literal (`red-team → opus`), and run the SAME checker entry point used for the live files against the copy — it MUST return a violation. This proves the production scan path catches drift, not just an isolated fixture string. *(Falsifier: fixture-path catches drift but the real-file path doesn't. Test: self — drifted-copy meta-test.)*
  - **Debug-ternary meta-test (pre-mortem r4 finding #2 — the hardest stage gets its own real-file meta-coverage):** also copy `gsd-t-debug.workflow.js` to temp paths and apply each of: (i) FLATTEN the ternary to a flat `model: "fable"` (cycle-1 wrongly fable), (ii) SWAP the branches (`cycle === 1 ? "fable" : "opus"`). Run the SAME checker entry point against each copy — each MUST return a violation. Additionally assert the debug mapping matches the live template-literal label `debug-cycle-${cycle}` with a non-zero match AND that the checker extracts BOTH ternary operands, asserting cycle-1→opus and cycle-2→fable as DISTINCT assertions (a loose regex that can't distinguish the cycles is decorative for this stage). *(Falsifier: a flattened or branch-swapped debug ternary passes the lint on a real-file copy. Test: self — two debug-copy meta-cases + operand-extraction assertion.)*
  - `npm test` is green: the negative + meta cases assert the checker rejects drift on fixtures/copies, not an actual suite failure. *(Test: `npm test`.)*

### M85-D4-T2 — Shadow A/B measurement (Fable single-draft vs 3-Opus competition) + MEASURED verdict in progress.md
- **Touches**: `.gsd-t/progress.md` (shadow-verdict block — measured numbers)
- **Files**: `.gsd-t/progress.md`
- **Test**: the standalone A/B harness defined below is the measurement instrument; the recorded verdict + run provenance is the evidence. Verification: `progress.md` carries three NON-PLACEHOLDER numeric fields + an explicit conclusion + BOTH run IDs, and the cited run artifacts exist on disk.
- **Contract refs**: `feedback_measure_dont_claim`, `.gsd-t/contracts/competition-mode-contract.md` v2.0.0 (the competition path being compared against)
- **Deps**: BLOCKED by M85-D3-T4 (stages must actually run on Fable before the shadow comparison is meaningful)
- **Acceptance criteria**:
  - **Step 0 — instrument precondition (pre-mortem r4 finding #1, scoped to the REAL artifact surface):** before the two A/B runs, prove a per-run token figure is actually extractable. The capture surface is NOT in the repo — it is the harness transcript store: `~/.claude/projects/-Users-david-projects-GSD-T/<session>/subagents/workflows/wf_<runId>/agent-*.jsonl` (+ `.meta.json`), which exists on disk (verified 2026-06-09: all five M85 plan-phase runs present, e.g. `wf_da3fb0fc-139`). The precondition: run the extractor against ONE real prior run's transcript dir and assert it returns a non-zero integer token total (sum of per-agent usage frames). The recorded verdict cites this artifact path + key, not the repo. **Documented fallback:** if no per-run figure is extractable from the transcript store, HOLD the measurement and record the instrument blocker LOUDLY — never fabricate numbers. *(Falsifier: the A/B verdict cites numbers no on-disk artifact yields. Test: extractor-returns-nonzero precondition on a real prior run before Run A starts.)*
  - **Concrete instrument (pre-mortem finding #2 — `gsd-t-audit` as shipped CANNOT do a model A/B; it is a component cost/benefit harness and its context-pct logging depends on the M61-retired `bin/token-budget.cjs`):** the measurement is a STANDALONE two-run harness on ONE eligible phase input: **Run A** — `gsd-t-phase.workflow.js` invoked with competition forced OFF and the single-draft stage on `fable` (via a temp script-variant copy with that one literal changed — a measurement-only artifact, never committed); **Run B** — the same input with `competition: 3` forced (producers `opus`, judge per shipped policy). Token/cost numbers come from the two runs' usage blocks (`subagent_tokens`) priced at $10/$50 (fable) vs $5/$25 (opus) per MTok; quality is scored by a BLIND referee agent applying the phase's rubric axes (from `RUBRIC_AXES_BY_PHASE`) to both artifacts, labels stripped, on a model that produced neither artifact (`sonnet`). *(Falsifier: no second run, or numbers not traceable to run artifacts. Test: both runs complete; usage blocks captured.)*
  - `progress.md` records: (i) per-axis quality scores for A and B (numeric), (ii) token delta (numeric, ≠ 0/N/A), (iii) cost delta in $ (numeric), plus an explicit one-line conclusion on whether Fable-single-draft matches judged-competition for eligible phases. *(Falsifier: any field missing, placeholder, or stated as a claim. Test: `grep` the three numeric components + conclusion.)*
  - **Provenance (anti-hand-typed-numbers):** the block cites BOTH workflow run IDs (`wf_…`) and their transcript dirs; the dirs MUST exist on disk and the cited token numbers MUST match the runs' usage blocks. A grep-passing block whose numbers trace to nothing FAILS this AC. *(Falsifier: hand-typed plausible numbers with no run artifacts. Test: artifact-existence check on the cited transcript dirs.)*

### M85-D4-T3 — Full doc ripple (one pass, gated on the measured verdict)
- **Touches**: `templates/CLAUDE-global.md`, `/Users/david/.claude/CLAUDE.md`, `CLAUDE.md` (project), `README.md`, `commands/gsd-t-help.md`, `.gsd-t/contracts/model-selection-contract.md`, `.gsd-t/progress.md`
- **Files**: `templates/CLAUDE-global.md`, `/Users/david/.claude/CLAUDE.md`, `CLAUDE.md`, `README.md`, `commands/gsd-t-help.md`, `.gsd-t/contracts/model-selection-contract.md`, `.gsd-t/progress.md`
- **Test**: `test/doc-ripple` consistency grep (template == live `~/.claude/CLAUDE.md` Model Display match) + `grep` each target carries the FABLE tier + cites the measured verdict; Document Ripple Completion Gate (no partial pass)
- **Contract refs**: `.gsd-t/contracts/model-selection-contract.md` (minor bump), `.gsd-t/contracts/model-tier-policy-contract.md` v1.0.0
- **Deps**: Requires M85-D4-T2 (within domain — the verdict must exist before docs cite it)
- **Acceptance criteria**:
  - The Model Display sections in `templates/CLAUDE-global.md` and live `~/.claude/CLAUDE.md` are updated IDENTICALLY (FABLE tier + the shipped single-source policy). *(Falsifier: template and live diverge. Test: diff/grep the two Model Display sections match.)*
  - Project `CLAUDE.md`, `README.md`, `commands/gsd-t-help.md` reflect the new tier + single-source tier policy. *(Falsifier: any of the three omits FABLE/policy. Test: `grep` each for the FABLE tier + policy-module reference.)*
  - `.gsd-t/contracts/model-selection-contract.md` is minor-version-bumped and cross-references `model-tier-policy-contract.md`, citing the measured verdict. *(Falsifier: version unchanged, or no cross-ref. Test: `grep` version line + cross-ref.)*
  - `progress.md`: Decision Log entry + the version-bump narrative 4.3.10 → 4.4.10 (minor). *(Falsifier: no Decision Log entry or no version narrative. Test: `grep` progress.md.)*
  - ALL updates land in ONE pass (Document Ripple Completion Gate — no "I updated 3 of 7"). *(Test: every file in **Touches** is modified in the same changeset.)*

## Execution Estimate
- Total tasks: 3
- Independent tasks (T1 once D1 lands): 1 (T1)
- Blocked tasks (within-domain + cross-domain): T2 needs D3-T4; T3 needs T2
- Estimated checkpoints: 2 (after T1 — lint guards D3's edits; after T3 — doc ripple complete)

## REQ Coverage
- AC (a) lint proves literals == policy, negative test mandatory → T1
- AC (e) shadow test MEASURED not claimed → T2
- Doc ripple (CLAUDE-global + live + project CLAUDE + README + help + model-selection-contract) → T3
- Version narrative 4.3.10 → 4.4.10 → T3
