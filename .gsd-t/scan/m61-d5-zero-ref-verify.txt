CHANGELOG.md:690:- **`bin/m44-proof-measure.cjs`** runs a falsifiable measurement of the v3.19.00 parallel dispatcher using a synthetic spawner (`test/fixtures/m44-proof/worker-sim.js`) injected into `runDispatch` via `opts.spawnHeadlessImpl`. Fixture (`test/fixtures/m44-proof/fixture.tasks.md`): 4 file-disjoint tasks with explicit `- touches:` sub-bullets (D5 disjointness requirement). Each worker sleeps `WORKER_DURATION_MS` (default 8000ms) then writes a JSON `.done` marker — zero LLM calls, zero network, zero side effects outside `OUT_DIR`. **Result**: T_par = **8111.1 ms**, T_seq = **32146.1 ms**, speedup **3.96×**, parallelism_factor **3.95** (ideal = 4), dispatch overhead **8.2 ms**. Criterion `T_par ≤ T_seq/2` → **MET ✓**. Report JSON at `.gsd-t/m44-proof-report.json`. This proves the dispatcher fans out concurrently; it does NOT prove N Claude workers produce correct code in T/N (a separate experiment, deferred to a follow-up backlog item).
test/m55-d3-ratelimit-probe.test.js:21:const probe = require('../bin/gsd-t-ratelimit-probe.cjs');
test/m55-d3-ratelimit-probe.test.js:22:const worker = require('../bin/gsd-t-ratelimit-probe-worker.cjs');
test/m40-speed-benchmark.test.js:16:const bench = require('../bin/gsd-t-benchmark-orchestrator.js');
.gsd-t/domains/m61-d5-retire-proof-scratch/scope.md:27:- `grep -rn "require.*m44-proof\|require.*m46-iter\|require.*m46-worker\|require.*m55-substrate\|require.*benchmark-orchestrator\|require.*parallel-probe\|require.*ratelimit-probe" --include='*.js' --include='*.cjs' --include='*.md' .` returns zero live references (these are confirmed orphans at partition time)
.gsd-t/progress-archive/018-2026-04-19.md:13:- 2026-04-19 19:50: [execute] M40 Wave 0 — D0 Task 1 landed (benchmark workload fixture). `test/fixtures/m40-benchmark-workload/` contains a self-contained `.gsd-t/` with one domain (`bench-d1`) and 4 tasks: W0-T1/T2 generate `out/{a,b}.txt` in parallel (independent writes), W1-T3/T4 derive uppercased `out/{c,d}.txt` each reading exactly one wave-0 artifact. Exercises both the parallel pool and the strict wave barrier. Carries its own `completion-signal-contract.md` copy so `buildTaskBrief` builds briefs without reaching into the parent repo — confirms the brief builder is project-scoped. README documents workload shape, invariants, and why 4 tasks / 2 waves is the right size (enough to exercise the orchestrator, small enough that orchestration overhead dominates model-throughput noise). 5/5 smoke tests pass (test/m40-benchmark-workload-fixture.test.js): layout present, `tasks.md` parses cleanly via D1 queue (4 tasks / 2 waves, no forward cross-wave deps), `buildTaskBrief` produces ≤10 KB briefs for all 4 tasks with Task + Done Signal sections. Full suite: **1323/1323** (was 1318, +5). **D0-T2 (benchmark driver) is next** but is intentionally operator-run — the driver spawns real `claude -p` processes to compare orchestrator vs in-session wall-clock, which requires live Claude auth and 3×N×~30 s of real workload per run. The driver skeleton + smoke-test wrapper (D0-T2 + D0-T3) can be built in-session; the PASS/FAIL verdict (`BENCHMARK: PASS|FAIL` → written to `docs/m40-benchmark-report.md`) comes from an operator running `gsd-t benchmark-orchestrator --runs 3`. If the verdict FAILs, the M40 plan is to halt before D4/D5.
.gsd-t/progress-archive/021-2026-04-22.md:31:- 2026-04-23 20:00 UTC: [measure · backlog #15 proof — T/2 dispatcher criterion MET, zero-compaction DEFERRED] Ran `bin/m44-proof-measure.cjs` against the v3.19.00 `runDispatch` instrument with a synthetic spawner (injected via `opts.spawnHeadlessImpl`) and 4 file-disjoint tasks in `test/fixtures/m44-proof/fixture.tasks.md`. Fixture required the explicit `- touches: fixture/X.txt` sub-bullet (D5 disjointness rule) — without it the planner returned `disjoint:false` and `workerCount:1`. Each synthetic worker (`test/fixtures/m44-proof/worker-sim.js`) sleeps `WORKER_DURATION_MS` (default 8000ms) then writes a JSON `.done` marker with `startedAt`/`endedAt`/`pid`/`durationMs`; zero LLM calls, zero network. **Numbers** (report at `.gsd-t/m44-proof-report.json`): `T_seq` (4 workers back-to-back) = **32146.1 ms**, `T_par` (runDispatch fan-out, 4 concurrent pids 59551–59554) = **8111.1 ms**, dispatch overhead = 8.2 ms, per-worker durations = `[8001, 8001, 8001, 8001]` ms, ratio `T_par/T_seq` = **0.252**, speedup = **3.96×**, parallelism_factor = **3.95** (ideal = 4), criterion `T_par ≤ T_seq/2` = **MET ✓**. This proves the **dispatcher** fans out concurrently; it does NOT prove N Claude workers produce correct code in T/N (a separate API-budget-intensive experiment, deferred). The dispatcher is what v3.19.00 ships, so its mechanics are what the tag certifies. **Zero-compaction criterion (unattended leg)**: `grep -c compaction_post_spawn .gsd-t/metrics/compactions.jsonl` = 0 across 81 rows, BUT the only post-19eb3eb unattended state in `.gsd-t/.unattended/state.json` is the 03:44 UTC M45 run (`status:failed, lastExit:6`) which predates the D9 panel landing. No unattended run has been launched under the v3.19.00-wired surface (`ca20477` supervisor→planner wire + `799a8af` single-instrument + `19eb3eb` D9 panel). Per user memory `feedback_measure_dont_claim.md` ("milestones with measurable success criteria are not complete until measurement is run AND reported"), the zero-compaction criterion is **NOT YET MEASURED** — `v3.19.00` tag DEFERRED until an unattended run under the fully-wired surface produces zero `type:compaction_post_spawn` rows over a workload that historically would have compacted. **Files (uncommitted, to ship alongside this entry)**: `test/fixtures/m44-proof/worker-sim.js` (new, synthetic worker), `test/fixtures/m44-proof/fixture.tasks.md` (new, 4 disjoint tasks), `bin/m44-proof-measure.cjs` (new, measurement driver), `.gsd-t/m44-proof-report.json` (measurement output). **Next**: launch an unattended supervisor over a real workload (M45 follow-up or a staged test workload) and record the resulting `.gsd-t/metrics/compactions.jsonl` window. Only then tag v3.19.00.
.gsd-t/milestones/M40-external-task-orchestrator-2026-04-20/progress.md:87:- 2026-04-19 19:50: [execute] M40 Wave 0 — D0 Task 1 landed (benchmark workload fixture). `test/fixtures/m40-benchmark-workload/` contains a self-contained `.gsd-t/` with one domain (`bench-d1`) and 4 tasks: W0-T1/T2 generate `out/{a,b}.txt` in parallel (independent writes), W1-T3/T4 derive uppercased `out/{c,d}.txt` each reading exactly one wave-0 artifact. Exercises both the parallel pool and the strict wave barrier. Carries its own `completion-signal-contract.md` copy so `buildTaskBrief` builds briefs without reaching into the parent repo — confirms the brief builder is project-scoped. README documents workload shape, invariants, and why 4 tasks / 2 waves is the right size (enough to exercise the orchestrator, small enough that orchestration overhead dominates model-throughput noise). 5/5 smoke tests pass (test/m40-benchmark-workload-fixture.test.js): layout present, `tasks.md` parses cleanly via D1 queue (4 tasks / 2 waves, no forward cross-wave deps), `buildTaskBrief` produces ≤10 KB briefs for all 4 tasks with Task + Done Signal sections. Full suite: **1323/1323** (was 1318, +5). **D0-T2 (benchmark driver) is next** but is intentionally operator-run — the driver spawns real `claude -p` processes to compare orchestrator vs in-session wall-clock, which requires live Claude auth and 3×N×~30 s of real workload per run. The driver skeleton + smoke-test wrapper (D0-T2 + D0-T3) can be built in-session; the PASS/FAIL verdict (`BENCHMARK: PASS|FAIL` → written to `docs/m40-benchmark-report.md`) comes from an operator running `gsd-t benchmark-orchestrator --runs 3`. If the verdict FAILs, the M40 plan is to halt before D4/D5.
.gsd-t/milestones/m44-2026-04-23/progress.md:207:- 2026-04-19 19:50: [execute] M40 Wave 0 — D0 Task 1 landed (benchmark workload fixture). `test/fixtures/m40-benchmark-workload/` contains a self-contained `.gsd-t/` with one domain (`bench-d1`) and 4 tasks: W0-T1/T2 generate `out/{a,b}.txt` in parallel (independent writes), W1-T3/T4 derive uppercased `out/{c,d}.txt` each reading exactly one wave-0 artifact. Exercises both the parallel pool and the strict wave barrier. Carries its own `completion-signal-contract.md` copy so `buildTaskBrief` builds briefs without reaching into the parent repo — confirms the brief builder is project-scoped. README documents workload shape, invariants, and why 4 tasks / 2 waves is the right size (enough to exercise the orchestrator, small enough that orchestration overhead dominates model-throughput noise). 5/5 smoke tests pass (test/m40-benchmark-workload-fixture.test.js): layout present, `tasks.md` parses cleanly via D1 queue (4 tasks / 2 waves, no forward cross-wave deps), `buildTaskBrief` produces ≤10 KB briefs for all 4 tasks with Task + Done Signal sections. Full suite: **1323/1323** (was 1318, +5). **D0-T2 (benchmark driver) is next** but is intentionally operator-run — the driver spawns real `claude -p` processes to compare orchestrator vs in-session wall-clock, which requires live Claude auth and 3×N×~30 s of real workload per run. The driver skeleton + smoke-test wrapper (D0-T2 + D0-T3) can be built in-session; the PASS/FAIL verdict (`BENCHMARK: PASS|FAIL` → written to `docs/m40-benchmark-report.md`) comes from an operator running `gsd-t benchmark-orchestrator --runs 3`. If the verdict FAILs, the M40 plan is to halt before D4/D5.
.gsd-t/milestones/M41-universal-token-capture-2026-04-20/progress.md:69:- 2026-04-19 19:50: [execute] M40 Wave 0 — D0 Task 1 landed (benchmark workload fixture). `test/fixtures/m40-benchmark-workload/` contains a self-contained `.gsd-t/` with one domain (`bench-d1`) and 4 tasks: W0-T1/T2 generate `out/{a,b}.txt` in parallel (independent writes), W1-T3/T4 derive uppercased `out/{c,d}.txt` each reading exactly one wave-0 artifact. Exercises both the parallel pool and the strict wave barrier. Carries its own `completion-signal-contract.md` copy so `buildTaskBrief` builds briefs without reaching into the parent repo — confirms the brief builder is project-scoped. README documents workload shape, invariants, and why 4 tasks / 2 waves is the right size (enough to exercise the orchestrator, small enough that orchestration overhead dominates model-throughput noise). 5/5 smoke tests pass (test/m40-benchmark-workload-fixture.test.js): layout present, `tasks.md` parses cleanly via D1 queue (4 tasks / 2 waves, no forward cross-wave deps), `buildTaskBrief` produces ≤10 KB briefs for all 4 tasks with Task + Done Signal sections. Full suite: **1323/1323** (was 1318, +5). **D0-T2 (benchmark driver) is next** but is intentionally operator-run — the driver spawns real `claude -p` processes to compare orchestrator vs in-session wall-clock, which requires live Claude auth and 3×N×~30 s of real workload per run. The driver skeleton + smoke-test wrapper (D0-T2 + D0-T3) can be built in-session; the PASS/FAIL verdict (`BENCHMARK: PASS|FAIL` → written to `docs/m40-benchmark-report.md`) comes from an operator running `gsd-t benchmark-orchestrator --runs 3`. If the verdict FAILs, the M40 plan is to halt before D4/D5.
.gsd-t/milestones/M42-live-spawn-transcript-viewer-2026-04-20/progress.md:87:- 2026-04-19 19:50: [execute] M40 Wave 0 — D0 Task 1 landed (benchmark workload fixture). `test/fixtures/m40-benchmark-workload/` contains a self-contained `.gsd-t/` with one domain (`bench-d1`) and 4 tasks: W0-T1/T2 generate `out/{a,b}.txt` in parallel (independent writes), W1-T3/T4 derive uppercased `out/{c,d}.txt` each reading exactly one wave-0 artifact. Exercises both the parallel pool and the strict wave barrier. Carries its own `completion-signal-contract.md` copy so `buildTaskBrief` builds briefs without reaching into the parent repo — confirms the brief builder is project-scoped. README documents workload shape, invariants, and why 4 tasks / 2 waves is the right size (enough to exercise the orchestrator, small enough that orchestration overhead dominates model-throughput noise). 5/5 smoke tests pass (test/m40-benchmark-workload-fixture.test.js): layout present, `tasks.md` parses cleanly via D1 queue (4 tasks / 2 waves, no forward cross-wave deps), `buildTaskBrief` produces ≤10 KB briefs for all 4 tasks with Task + Done Signal sections. Full suite: **1323/1323** (was 1318, +5). **D0-T2 (benchmark driver) is next** but is intentionally operator-run — the driver spawns real `claude -p` processes to compare orchestrator vs in-session wall-clock, which requires live Claude auth and 3×N×~30 s of real workload per run. The driver skeleton + smoke-test wrapper (D0-T2 + D0-T3) can be built in-session; the PASS/FAIL verdict (`BENCHMARK: PASS|FAIL` → written to `docs/m40-benchmark-report.md`) comes from an operator running `gsd-t benchmark-orchestrator --runs 3`. If the verdict FAILs, the M40 plan is to halt before D4/D5.
