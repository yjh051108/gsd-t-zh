# GSD-T Progress

## Project: GSD-T Framework (@tekyzinc/gsd-t)
## Status: ACTIVE — M57 RE-PLANNING (design halted by Red Team)
## Date: 2026-05-19
## Version: 3.26.11

## Current Milestone

M57 — CI-Parity Verify Gate. **RE-PLANNING 2026-05-19** — first design halted. Red Team ran 5 cycles without converging: D1 substring-match build-coverage is structurally false-negative-prone (BUG-4→6→9→9b — each fix spawned a syntactic variant where the dir name appears as prose, not a build input), and D2's `clearBuildCaches` had 3 CRITICAL Destructive-Action-Guard violations (rmSync outside projectDir on `outDir:"../x"`, and project-root delete on `outDir:"."`). VERDICT: FAIL. Autonomous chain halted (Prime Rule stop #2). Re-plan mandate (user-approved): **D1 parses CI files structurally** (COPY/ADD args + cloudbuild `args[]` + workflow `run:` as *paths*, never `text.includes(dir)`); **D2 cache-clear hard-refuses any path resolving outside OR equal-to projectDir** (predicate `resolved.startsWith(root+sep) && resolved!==root`). Falsification corpus preserved at `test/fixtures/m57-build-coverage/bug*/` (commit 56ddded) — the corrected structural parser MUST pass every variant. red-team-report.md + qa-issues.md (16 findings/5 CRITICAL) retained as re-plan input. Memory: `feedback_coverage_check_structural_not_substring.md`, `feedback_destructive_path_ops_containment.md`. Flawed `.cjs` reset to execute-commit 65c2d4a (clean impl slate). Goal unchanged: `gsd-t-verify` reproduces the project's actual CI build. Origin: TimeTracking v1.10.12 post-mortem.

## Milestones

| # | Milestone | Status | Version | Domains |
|---|-----------|--------|---------|---------|
| M57 | CI-Parity Verify Gate | PLANNED | TBD | 2 file-disjoint domains. **D1 build-coverage-check**: `bin/gsd-t-build-coverage.cjs::checkBuildCoverage({projectDir, baseRef, headRef})` enumerates new top-level paths in the milestone commit range; verifies each is referenced by a `COPY`/`ADD` in `Dockerfile`, an artifact/copy step in `cloudbuild.yaml`, or a path in `.github/workflows/*.yml`; returns `{ok, missing[]}`. CLI `gsd-t build-coverage`. Contract `cli-build-coverage-contract.md`. **D2 ci-command-parity**: `bin/gsd-t-ci-parity.cjs` auto-detects project CI config (`cloudbuild.yaml` steps[].args → `.github/workflows/*.yml` jobs[].steps[].run → `Dockerfile` RUN lines → fallback `package.json` scripts build/typecheck/test), runs exactly those commands locally with build caches cleared (`.tsbuildinfo`, `node_modules/.cache`, tsc incremental), and auto-runs `docker build` when a `Dockerfile` is present (presence is the trigger — no opt-in flag). Contract `ci-parity-contract.md`. **Wire-in**: both become mandatory checks in `commands/gsd-t-verify.md` — failure = verify FAIL (blocks complete-milestone), never warning-only. `templates/CLAUDE-global.md` Pre-Commit Gate updated to reference that CI-parity is enforced by `gsd-t-verify` (not a new self-attested checklist item). **Pair-flag**: backlog #25 (`gsd-t-bench`) canonical eval set must include a synthetic regression of this exact incident when bench ships. **Decisions locked (user)**: CI detection = auto-detect from project files; docker build = auto-run when Dockerfile detected. **SCs**: SC1 `checkBuildCoverage` flags a new-dir-not-COPY'd fixture (the TimeTracking class) with `missing[]` non-empty + exit 4; SC2 `ci-parity` on a project with a Dockerfile runs the real `docker build` and fails on a planted tsc strict regression that a warm-cache local `tsc` would miss; SC3 verify FAILs (blocks complete-milestone) when either check fails — proven by deliberately-broken fixture; SC4 zero regressions on `npm test`; SC5 Red Team GRUDGING PASS ≥5 broken patches caught; SC6 doc-ripple complete (verify.md + CLAUDE-global.md + GSD-T-README + README + help + contracts). **Pre-partition assessment**: 2 file-disjoint domains (D1 `bin/gsd-t-build-coverage.cjs` + contract; D2 `bin/gsd-t-ci-parity.cjs` + contract; shared touch only `commands/gsd-t-verify.md` + `bin/gsd-t.js` CLI dispatch — sequenced at integrate, not parallel-written). 1 parallel wave (D1∥D2) + integrate + verify. Standard partition → plan → execute → integrate → verify flow; no discussion needed (pain pattern + surface well-understood from the post-mortem). |
| M56 | Verify-Gate CLI Fan-Out + Upper-Stage Briefs | COMPLETE | 3.26.10 | 5 file-disjoint domains. **D1 verify-gate-native-cli-workers**: extend `bin/gsd-t-verify-gate.cjs` Track 2 with native `playwright test`, `npm test`, `gsd-t check-coverage` workers (no Task subagent wrapper); record `M55-baseline-tokens` and `M56-actual-tokens` in `.gsd-t/metrics/` + CHANGELOG delta (closes M55 SC4 retroactively). **D2 upper-stage-brief-kinds**: extend `bin/gsd-t-context-brief.cjs` with 5 new kinds: `partition`, `plan`, `discuss`, `impact`, `milestone` (currently only `execute`/`verify`/`qa`/`red-team`/`design-verify`/`scan`). **D3 upper-stage-command-wirein**: thread brief into `commands/gsd-t-{partition,plan,discuss,impact,milestone}.md` Step 1 via `$BRIEF_PATH` pattern from M55 D4. **D4 quick-debug-wirein**: wire preflight + brief + verify-gate into `commands/gsd-t-quick.md` and `commands/gsd-t-debug.md`. **D5 stream-json-universality-lint**: 3 known gaps (`bin/gsd-t.js:3879 spawnClaudeSession()`, `bin/gsd-t-parallel.cjs:378` cache-warm probe, `bin/gsd-t-ratelimit-probe-worker.cjs:89`) — add `--output-format stream-json --verbose` to all three; extend `gsd-t capture-lint` (or sibling) to mechanically reject any `claude -p` / `spawn('claude', …)` invocation without the flag pair; allowlist (e.g. probe workers) lives in lint config not tribal knowledge. **Falsifiable SCs**: SC1 verify wall-clock < M55's 34s; SC2 first-of-milestone brief shaves 30–60k from each subsequent phase's read-budget (measured); SC3 quick+debug invocations show new mandatory preflight banner; SC4 new lint blocks deliberately broken commit dropping `--output-format stream-json`; SC5 (M55 SC4 retroactive closure) M56 records baseline+actual tokens delta in CHANGELOG; SC6 zero regressions on `npm test`; SC7 Red Team GRUDGING PASS — ≥5 broken patches all caught. **Probe learnings honored**: Sonnet ≥11 / Opus ≥7 cliff-clean on 30k prose fixtures; `peakConcurrency=12` in `.gsd-t/ratelimit-map.json`; probe v3+ classifies 429s via API result envelope NOT regex (M56 must NOT regress to regex). **Versioning**: minor bump 3.25.11 → 3.26.10 on complete-milestone (new feature). **Test baseline**: 2487/2485 unit (M55 carryover, 2 pre-existing env-bleed). **Files (planned, finalized at partition)**: `bin/gsd-t-verify-gate.cjs` (additive Track 2 native workers + token-delta recording), `bin/gsd-t-context-brief.cjs` (5 new kinds), `commands/gsd-t-{partition,plan,discuss,impact,milestone,quick,debug}.md` (additive brief + preflight + verify-gate Step 1), `bin/gsd-t.js` (`spawnClaudeSession` add stream-json), `bin/gsd-t-parallel.cjs:378` (cache-warm probe add stream-json), `bin/gsd-t-ratelimit-probe-worker.cjs:89` (add stream-json), `bin/gsd-t-capture-lint.cjs` (additive `--check-stream-json` mode OR new sibling `bin/gsd-t-stream-json-lint.cjs`), `.gsd-t/metrics/m56-token-baseline.json` (new), `e2e/journeys/m56-*.spec.ts` (new ≥3 specs). **Pre-partition assessment**: medium-complexity (5 file-disjoint domains; D5 lint is the riskiest because it must allowlist probe workers without reopening the loophole). Recommend standard partition → plan → execute flow; no discussion needed (pain pattern + surface well-understood from M55 D4/D5 + M41 capture-lint precedent). |
| M55 | CLI-Preflight Pattern + Parallel-CLI Substrate + Rate-Limit Map + Context Briefs + Verify Gate | COMPLETE | 3.25.10 | 5 file-disjoint domains, all shipped. D1 cli-preflight (commit bb514db), D2 parallel-cli substrate + 5.57× speedup proof (7b9e013), D3 ratelimit probe + map (a0bef17), D4 context-brief generator (998f373), D5 verify-gate two-track + wire-in + doc ripple (b916d77). Red Team GRUDGING PASS — 6/6 broken patches caught (fce2f18). Verify-gate dogfood: 34s wall-clock vs trailing-3 median 681s (20× margin). 2487/2485 tests (2 pre-existing env-bleed). 8 SCs final: SC1✅ contract STABLE, SC2✅ 5.57× speedup, SC3✅ 3 e2e SC3 specs, SC4⚠️ deferred-by-instrumentation-gap (trailing-3 lacks comparable token totals; M56 first measurement closes retroactively), SC5✅ Sonnet ≥11 / Opus ≥7 measured (peakConcurrency bumped 8→12 in ratelimit-map.json), SC6✅ verify-gate 34s ≤ 340s threshold, SC7✅ Red Team GRUDGING PASS, SC8✅ zero regressions. Archive: `.gsd-t/milestones/m55-cli-preflight-pattern-2026-05-09/`. Charter archived. Contracts copied to archive (still referenced in active `.gsd-t/contracts/`). |
| M52 | Rigorous User-Journey Coverage + Anti-Drift Test Quality | COMPLETE | 3.23.10 | 2 file-disjoint domains. **D1 m52-d1-journey-coverage-tooling** (5 tasks): `bin/journey-coverage.cjs` listener-detector + `bin/journey-coverage-cli.cjs` (`gsd-t check-coverage`) + `scripts/hooks/pre-commit-journey-coverage` + `bin/gsd-t.js` wiring (`installJourneyCoverageHook`, `--install-journey-hook`) + `journey-coverage-contract.md` (manifest schema + listener catalogue + gap rules + exit codes). **D2 m52-d2-journey-specs-and-fixtures** (5 tasks): 12 inaugural `e2e/journeys/*.spec.ts` + `.gsd-t/journey-manifest.json` + 3 real-data NDJSON fixtures under `e2e/fixtures/journeys/` + `replay-helpers.ts` + Red Team "Test Pass-Through — Journey Edition" category. **Cross-domain**: only `bin/gsd-t.js` touched by both — D1 only writes it; D2 only reads. **Checkpoints** (`m52-integration-points.md`): C1 D1 publishes `gsd-t check-coverage` + hook installer + STABLE contract → unblocks D2 manifest authoring; C2 D2 lands 12 specs + populated manifest → unblocks verify; C3 Red Team adversarial pass → unblocks complete-milestone. |
| M50 | Universal Playwright Bootstrap + Deterministic UI Enforcement | COMPLETE | 3.22.10 | 2 domains file-disjoint, single-day in-session build. D1 (5 tasks): `bin/playwright-bootstrap.cjs` (`hasPlaywright`/`detectPackageManager`/`installPlaywright`/`installPlaywrightSync`/`verifyPlaywrightHealth`) + `bin/ui-detection.cjs` (`hasUI`/`detectUIFlavor`) + `bin/gsd-t.js` wiring (init/update-all/doctor/setup-playwright). D2 (10 tasks): spawn-gate in `bin/headless-auto-spawn.cjs` + `scripts/hooks/pre-commit-playwright-gate` + `playwright.config.ts` + 5 E2E viewer specs + doc-ripple + contract flip to STABLE. 62 new unit tests; E2E 8/9 pass (1 documented TEST-M50-001). Goal-Backward PASS (16 REQs). Zero regressions. |
| M47 | Focused Visualizer Redesign | COMPLETE | 3.21.10 | 2 domains, 12 tasks, single-day in-session build. D1 viewer-redesign (7 tasks): split-pane HTML scaffolding (`#main-stream` + splitter + `#spawn-stream`); 3-section left rail (Main Session / Live / Completed[≤100, newest first, status-badged, collapsible]); dual SSE wiring via new `connectMain(sessionId)` + bottom-pane `connect()`; bucketing logic consumes D2 `status` field; mouse + keyboard splitter (ArrowUp/Down ±5%, Home/End snap); 4 sessionStorage keys (`selectedSpawnId`, `splitterPct`, `completedExpanded`, `rightRailCollapsed`); right-rail collapse toggle; `appendFrame`/`renderFrame` refactored to thread an optional target via module-scope `renderTarget`. D2 server-helpers (5 tasks): 30s-window `status` field on `listInSessionTranscripts`; new `handleMainSession` + `GET /api/main-session` route (path-traversal guarded via `isValidSpawnId`, `Cache-Control: no-store`); `dashboard-server-contract.md` v1.2.0 → v1.3.0; 9 regression tests. Suite 2058/2060 pass (baseline 2045 + 13 M47 new; 2 pre-existing flakes preserved per success-criterion-5). Goal-Backward PASS (0 placeholder patterns). Red Team grudging pass (13 attack vectors, 0 bugs). |
| M46 | Unattended Iter-Parallel + Worker Fan-Out Completion | COMPLETE | 3.19.00 | 2 domains, single parallel wave, file-disjoint. D1 iter-parallel supervisor (helpers `_runOneIter`/`_computeIterBatchSize`/`_runIterParallel`/`_reconcile` in `bin/gsd-t-unattended.cjs` + `iter-parallel-contract.md` v1.0.0 + `test/m46-d1-iter-parallel.test.js` + `bin/m46-iter-proof.cjs` @ 3.35× speedup). D2 worker-side runDispatch hand-off (new `bin/gsd-t-worker-dispatch.cjs` + `commands/gsd-t-resume.md` additive block + `test/m46-d2-worker-subdispatch.test.js` + `bin/m46-worker-proof.cjs` @ 5.96× speedup + `headless-default-contract.md` v2.0.0 → v2.1.0). Closes five-surface audit gaps 2A (✗ → ✓) and 2B (⚠ → ✓). Iter-parallel scaffold production-default serial; engaged via opt-in `opts.maxIterParallel` — full concurrent-safe work-stealing tracked as backlog #24. |
| M45 | Conversation-Stream Observability | COMPLETE | 3.18.14 | 2 domains, Wave 1 parallel (file-disjoint test fixture for M44 parallelism). D1 viewer-route-fix (4 tasks, 4/4 tests — `GET /transcripts` now serves `gsd-t-transcript.html` with empty `__SPAWN_ID__`). D2 in-session-conversation-capture (6 tasks, 20/20 tests — new `scripts/hooks/gsd-t-conversation-capture.js`; compact-detector fallback target-selection; viewer left-rail `💬 conversation` badge; `conversation-capture-contract.md` v1.0.0). Red Team found BUG-1 MEDIUM (session_id path-traversal); sanitizer fix + regression test landed. |
| M44 | Cross-Domain & Cross-Task Parallelism (in-session AND unattended) | COMPLETE | 3.18.10 | 8 of 9 domains. Task-graph reader, `gsd-t parallel` CLI, file-disjointness prover, pre-spawn economics, spawn-plan visibility, per-CW attribution, command-file integration. |
| M43 | Token Attribution & Always-Headless Inversion | COMPLETED | 3.17.10 | 6 domains across 2 themes. Part A: per-turn in-session usage capture + per-tool attribution + sink unification. Part B: always-headless inversion (channel separation). Contracts: metrics-schema v2, tool-attribution v1.0.0, headless-default v2.0.0. |
| M42 | Live Spawn Transcript Viewer | COMPLETE | 3.16.10 | 3 domains (stream-json tee + SSE renderer + sidebar/kill). |
| M41 | Universal Token Capture Across GSD-T | COMPLETE | 3.15.10 | 5 domains. `captureSpawn`/`recordSpawnRow` wrapper + doc-ripple + backfill + dashboard + lint enforcement. |
| M40 | External Task Orchestrator + Streaming Watcher UI | COMPLETE | 3.14.10 | 7 domains, 30 tasks. D0 operator wall-clock 0.72× vs in-session. |
| M39 | Fast Unattended + Universal Watch-Progress Tree | COMPLETE | 3.13.10 | 3 domains + 6 follow-on patches. |
| M38 | Headless-by-Default + Meter Reduction | COMPLETE | 3.12.10 | Headless spawn default across 7 commands; single-band meter; smart router. |
| M37 | Universal Context Auto-Pause | COMPLETE | 3.11.10 | Strengthened context meter MANDATORY STOP across 5 loop commands. |
| M36 | Cross-Platform Unattended Supervisor Loop | COMPLETE | 3.10.10 | Detached supervisor relay; 3 new slash commands; ScheduleWakeup(270s) watch loop. |
| M35 | No Silent Degradation + Surgical Model Escalation + Token Telemetry | COMPLETE | 2.76.10 | 7 domains, 38 tasks. |
| M34 | Context Meter | COMPLETE | 2.75.10 | Anthropic count_tokens API; replaces task-counter proxy. |

## Completed Milestones

| Milestone | Version | Completed | Tag | Summary |
|-----------|---------|-----------|-----|---------|
| M54 Live Activity Visibility | 3.24.10 | 2026-05-07 | v3.24.10 | 2 file-disjoint domains, 8 tasks total, single-day in-session build. **D1 server-and-detector** (5 tasks): `bin/live-activity-report.cjs` (new, 615 LOC, pure read-only, zero deps) — `computeLiveActivities({projectDir,now?})` detects 4 kinds (bash run_in_background, Monitor tool_use without stop, tool_use >30s, spawn-plan endedAt:null); 3 falsifiers (F1 terminator event, F2 process.kill PID ESRCH, F3 source-file mtime >60s); cross-stream dedup by tool_use_id then (kind,label,startedAt) tuple; UNION of `.gsd-t/events/<today>.jsonl` + `~/.claude/projects/<slug>/<sid>.jsonl`. 3 dashboard handlers added to `scripts/gsd-t-dashboard-server.js` (additive — handleLiveActivity 5s cache, handleLiveActivityTail with isValidActivityId path-traversal guard, handleLiveActivityStream SSE 15s heartbeat). 1-line `GLOBAL_BIN_TOOLS` edit in `bin/gsd-t.js`; module hot-patched to `~/.claude/bin/`; `gsd-t doctor` reports "All 2 global bin tools installed". `live-activity-contract.md` flipped v0.1.0 PROPOSED → v1.0.0 STABLE. **D2 rail-and-spec** (3 tasks): additive `<section id="rail-live-activity">` + CSS `@keyframes accent-pulse` (.la-pulsing class) + `wireLiveActivity()` IIFE in `scripts/gsd-t-transcript.html` (5s polling, appendActivity/removeActivity/updateDuration/loadTailUrl/stopPulse, 3 pulse-stop conditions: click/absence/30s); 2 live-journey specs under `e2e/live-journeys/` (single-bash + multikind, both self-skip when no live dashboard); manifest +2 entries. All 3 checkpoints PUBLISHED. Red Team GRUDGING PASS: 5/5 broken patches caught (dedupe-disabled, PID-stub-true, mtime-fallback-removed, pulse-never-clears, tool_use_id-collision-unhandled). Suite **2262/2262 unit pass** (baseline 2233 + 29 M54 D1 new); Playwright **39 pass + 23 self-skip** in 2.6s. `gsd-t check-coverage` reports `OK: 21 listeners, 16 specs` exit 0. Goal-Backward PASS (12 REQs, 0 placeholders). |
| M52 Rigorous User-Journey Coverage + Anti-Drift Test Quality | 3.23.10 | 2026-05-06 | v3.23.10 | 2 file-disjoint domains, 10 tasks total (5 D1 + 5 D2), single-day in-session build. D1 journey-coverage tooling: `bin/journey-coverage.cjs` (308 lines, regex listener detector, zero parser deps, sub-100ms) + `bin/journey-coverage-cli.cjs` (`gsd-t check-coverage` CLI, exit 0/4/2) + `scripts/hooks/pre-commit-journey-coverage` (mode 0755, marker-block idiom) + `bin/gsd-t.js` wiring (+46 lines: `installJourneyCoverageHook`, `check-coverage` dispatch, `--install-journey-hook` doctor flag, auto-install in init) + `journey-coverage-contract.md` v1.0.0 STABLE. D2 journey specs + fixtures: 12 inaugural `e2e/journeys/*.spec.ts` covering all 20 detected viewer listeners — every assertion verifies user-visible state change (zero `toBeVisible`/`toBeAttached` shallow assertions); `.gsd-t/journey-manifest.json` 12 entries; 3 real-data NDJSON fixtures + `replay-helpers.ts`; Red Team "Test Pass-Through — Journey Edition" additive category in `templates/prompts/red-team-subagent.md`. Red Team GRUDGING PASS: 5/5 broken viewer patches caught; hook end-to-end exercised (block-then-unblock). Suite: 2195/2195 unit + 35/35 E2E + 1 skip preserved. `gsd-t check-coverage` reports `OK: 20 listeners, 12 specs` (exit 0). |
| M50 Universal Playwright Bootstrap + Deterministic UI Enforcement | 3.22.10 | 2026-05-06 | v3.22.10 | 2 domains file-disjoint, single-day in-session build. Three executable enforcement layers: (1) `bin/playwright-bootstrap.cjs` + `bin/ui-detection.cjs` wired into init/update-all/doctor/setup-playwright; (2) spawn-gate in `bin/headless-auto-spawn.cjs` (auto-install on hasUI && !hasPlaywright, blocked-needs-human on fail); (3) `scripts/hooks/pre-commit-playwright-gate` (opt-in commit gate). Also ships `playwright.config.ts` + 5 E2E viewer specs (M48 Bug 1-4 regressions + M49 banner). 62 new unit tests; E2E 8/9 pass. Goal-Backward PASS. Zero regressions. |
| M47 Focused Visualizer Redesign | 3.21.10 | 2026-05-06 | v3.21.10 | 2 domains file-disjoint, single-day in-session build. D1 viewer-redesign (7 tasks): split-pane HTML + 3-section rail (Main/Live/Completed≤100) + dual SSE + mouse/keyboard splitter + 4 sessionStorage persistence keys + right-rail collapse + reactive Live→Completed transition. D2 server-helpers (5 tasks): 30s-window `status` field on `listInSessionTranscripts` + new `GET /api/main-session` (path-traversal guarded, no-cache) + contract v1.3.0 + 9 regression tests. Suite 2058/2060 (M47 +13/+13, 2 pre-existing flakes preserved). Goal-Backward PASS, Red Team grudging pass (0 bugs across 13 vectors). |
| M46 Unattended Iter-Parallel + Worker Fan-Out Completion | 3.19.00 | 2026-04-23 | v3.19.00 | 2 domains file-disjoint, same-day build. D1 iter-parallel scaffold + contract v1.0.0 + 12 unit tests + proof 3.35×; D2 worker sub-dispatch production path + contract v2.0.0 → v2.1.0 + 6 unit tests + proof 5.96×. Closes audit surfaces 2A + 2B. Production default gated serial pending concurrent-safe work-stealing rewrite (backlog #24). Full suite 1946/1946 pass, zero regressions. |
| M45 Conversation-Stream Observability | 3.18.14 | 2026-04-23 | v3.18.14 | 2 domains, parallel Wave 1. D1 viewer-route-fix; D2 in-session-conversation-capture with contract v1.0.0. Red Team caught session_id path-traversal (fix + regression test). |
| M44 Cross-Domain & Cross-Task Parallelism | 3.18.10 | 2026-04-23 | v3.18.10 | 8 of 9 domains. Task-graph, `gsd-t parallel` CLI, disjointness prover, economics estimator, spawn-plan visibility, per-CW attribution. |
| M43 Token Attribution & Always-Headless Inversion | 3.17.10 | 2026-04-21 | v3.17.10 | 6 domains across 3 waves. Part A universal token attribution; Part B always-headless channel separation. |
| M42 Live Spawn Transcript Viewer | 3.16.10 | 2026-04-20 | v3.16.10 | 3 domains. Claude-Code-style per-spawn transcript on `:7433`. |
| M41 Universal Token Capture Across GSD-T | 3.15.10 | 2026-04-20 | v3.15.10 | 5 domains. `bin/gsd-t-token-capture.cjs` + lint enforcement. |
| M40 External Task Orchestrator + Streaming Watcher UI | 3.14.10 | 2026-04-20 | v3.14.10 | 7 domains. Operator 0.72× wall-clock. |
| M39 Fast Unattended + Universal Watch-Progress Tree | 3.13.10 | 2026-04-18 | v3.13.10 | 3 domains + 6 patches. |
| M38 Headless-by-Default + Meter Reduction | 3.12.10 | 2026-04-17 | v3.12.10 | Default headless across 7 commands. |
| M37 Universal Context Auto-Pause | 3.11.10 | 2026-04-16 | v3.11.10 | MANDATORY STOP across 5 commands. |
| M36 Cross-Platform Unattended Supervisor Loop | 3.10.10 | 2026-04-15 | v3.10.10 | Detached supervisor relay. |
| M35 Runway-Protected Execution | 2.76.10 | 2026-04-15 | v2.76.10 | Surgical model selection. |
| M34 Context Meter | 2.75.10 | 2026-04-14 | v2.75.10 | Real CW measurement. |

Older milestones (M33 and earlier) archived under `.gsd-t/milestones/` — see directory listing for the full index.

## Blockers

<!-- No active blockers -->

## Decision Log

- 2026-05-19: [halt+re-plan] M57 first design HALTED by Red Team, re-planning approved. Detached chain (pid 37211) reached post-execute Red Team, which ran **5 cycles without converging** — protocol allows 2. Root cause = design defect, not patchable bug: **D1** answers "is new dir covered?" by substring-matching the dir name against CI-file text; every fix-cycle found a new variant where the name appears as prose not a build input (BUG-4 interior token → BUG-6 comment → BUG-9 GHA step `name:` → BUG-9b block/folded scalar). **D2** `clearBuildCaches` shipped 3 CRITICAL Destructive-Action-Guard violations: BUG-1 `fs.rmSync` recursive-force OUTSIDE projectDir via `tsconfig outDir:"../victim"`; BUG-8 (regression from BUG-1 fix) force-deletes the ENTIRE project when `outDir` resolves to root (`"."`,`"./"`,`"src/.."`). Red Team VERDICT: FAIL (16 findings: 5 CRITICAL/6 HIGH/3 MEDIUM/4 LOW). Chain terminated (Prime Rule stop #2 — design flaw fundamentally changes direction; not a 2-attempt error loop). Full suite stayed green (2606/2606) *while the gate was broken* — same failure mode M57 exists to catch. User chose: re-plan D1+D2 with corrected design. **Corrected mandate**: D1 parses CI structure (COPY/ADD args, cloudbuild `args[]`, workflow `run:` as paths); D2 containment predicate `resolved.startsWith(root+path.sep) && resolved!==root`. Falsification corpus (`test/fixtures/m57-build-coverage/bug*/`) + red-team-report.md + qa-issues.md preserved as re-plan input (commit 56ddded). Flawed `.cjs`+tests reset to execute-commit 65c2d4a. Lessons → memory: `feedback_coverage_check_structural_not_substring.md`, `feedback_destructive_path_ops_containment.md`. Next: re-run /gsd-t-plan with corrected D1/D2 design, then execute → integrate → verify → complete-milestone.

> Prior decision log entries preserved in `.gsd-t/milestones/*/progress.md` — see archive snapshots for pre-M51 history.

- 2026-05-18: [plan] M57 planned — 10 tasks (D1: T1 fixtures → T2 detector+parsers → T3 CLI → T4 tests/SC1 → T5 contract STABLE; D2: T1 fixtures → T2 detect+cache-clear+runner → T3 auto-docker → T4 CLI+tests/SC2 → T5 contract STABLE). 1 parallel wave D1∥D2 (zero cross-domain execute-time coupling; shared files gsd-t.js + gsd-t-verify.md integrate-sequenced only). `gsd-t parallel --dry-run` confirms M57-D1-T1 ∥ M57-D2-T1 disjoint+parallel. No duplicate operations across domains; no SharedCore. Test verification: 2547/2547 baseline holds. Auto-advancing to execute.

- 2026-05-18: [partition] M57 partitioned into 2 file-disjoint domains. **D1 m57-d1-build-coverage-check**: owns `bin/gsd-t-build-coverage.cjs` + `cli-build-coverage-contract.md` (DRAFT) + tests/fixtures. **D2 m57-d2-ci-command-parity**: owns `bin/gsd-t-ci-parity.cjs` + `ci-parity-contract.md` (DRAFT) + tests/fixtures. Shared files (`bin/gsd-t.js` dispatch + `commands/gsd-t-verify.md` FAIL-blocking wire-in) are integrate-sequenced, NOT in the parallel wave (both domains' wire-ins touch them). 1 parallel wave D1∥D2 → integrate → verify → complete-milestone. Assumption audit: TimeTracking = INSPECT-only (post-mortem facts define fixtures, no vendoring); CI formats = BUILD minimal parsers (no YAML lib, zero-dep invariant); both user decisions (auto-detect CI / docker-on-presence) LOCKED — not re-asked. Single CLI consumer class → no SharedCore. No UI signals → design brief/contract skipped. Test baseline confirmed 2547/2547 on branch m57-ci-parity-verify-gate. Pair-flag backlog #25 recorded in m57-integration-points.md. Level 3 — auto-advancing to plan.

- 2026-05-18: [defined] M57 — CI-Parity Verify Gate DEFINED. Origin: TimeTracking v1.10.12 post-mortem — `gsd-t-verify` reported VERIFIED, milestone tagged + pushed, but Cloud Build (commit 189a91f) failed: new top-level `hooks/` dir committed but absent from Dockerfile COPY directives + ~8 noImplicitAny regressions passed local tsc (stale incremental cache / divergent tsconfig) yet failed CI. Root cause = verify never reproduces the project's real CI build. Cross-project survey: 7/18 registered projects have CI surfaces (4 Docker+CloudBuild, 3 GHA-only) — systemic, not project-specific → GSD-T defect. Scope = 2 file-disjoint domains (D1 build-coverage-check, D2 ci-command-parity), both wired into `gsd-t-verify` as FAIL-blocking checks. User decisions locked: auto-detect CI from project files; auto-run `docker build` when Dockerfile present (no opt-in flag). Pair-flagged with backlog #25 (gsd-t-bench eval set). Level 3 Full Auto — chaining partition → plan → execute → integrate → verify → complete-milestone.

- 2026-05-13 13:15 PDT: [backlog] Added item #25: "gsd-t-bench — A/B eval harness for methodology changes" (type: feature, app: gsd-t, category: commands). Covers 4 use cases (command-edit A/B, model routing eval, stack-rule effectiveness, pre-publish regression gate) + pre-commit gate trigger on commands/** and templates/** edits. Pattern adapted from anthropics/skills `skill-creator` eval harness (reviewed same day). Sized 2-3 domains. Defer-until M44 cross-domain parallelism integrated.

- 2026-05-09 20:55 PDT: [completed] M56 — Verify-Gate CLI Fan-Out + Upper-Stage Briefs COMPLETE. Version 3.25.11 → **3.26.10** (minor bump per charter — new feature). **Verify**: 33975ms wall-clock < 34000ms M55 baseline (SC1 PASS, margin 25ms). All 3 new Track 2 native workers (`tests`, `playwright`, `journey-coverage`) ran ok. **Red Team GRUDGING PASS**: 6 attacks applied, 4 caught cleanly, 2 partial gaps documented as follow-on backlog (PATH integrity in `_hasOnPath`, `--verbose` not separately enforced in stream-json lint). 0 CRITICAL / 0 HIGH / 0 MEDIUM bugs. **SC scoreboard**: SC1✅ wall-clock < 34000ms (33975ms), SC2✅ 5 briefs all under 10 KB cap (7-13× smaller than full reads), SC3✅ Quick + Debug carry preflight banner, SC4✅ deliberately-broken-commit blocks exit 4 with file:line, SC5⚠️ deferred (token-delta requires fan-out execute; in-session-serial milestone didn't exercise it; closure path: next parallel-fan-out execute milestone), SC6✅ 2547/2547 tests pass (zero regressions), SC7✅ Red Team GRUDGING PASS. **Verify report**: `.gsd-t/verify-report.md`. **Archive**: `.gsd-t/milestones/m56-verify-gate-cli-fanout-2026-05-09/` (5 domains + integration-points + verify-report + metrics scaffolds copied; active `.gsd-t/contracts/m56-integration-points.md` remains in-place per archive convention). **CHANGELOG**: `[3.26.10] - 2026-05-09` entry added with full SC scoreboard + process notes (plan-tooling repair lesson captured in `feedback_plan_for_parallel_execution.md` memory).

- 2026-05-09 20:48 PDT: [success] M56 D1+D2+D3+D4 COMPLETE — verify-gate native CLI workers + 5 new brief kinds + 4 upper-stage command wire-ins + quick+debug wire-ins shipped. **D1**: `bin/gsd-t-verify-gate.cjs::_detectDefaultTrack2` extended with 2 new native CLI workers (`playwright` when `playwright.config.{ts,js,cjs}` present, `journey-coverage` when `.gsd-t/journey-manifest.json` present) + `.gsd-t/metrics/m56-token-baseline.json` + `.gsd-t/metrics/m56-verify-gate-wallclock.json` (M55 baseline = $21.84 / 34000ms; M56 actual TBD at verify) + 7 unit tests in `test/m56-d1-verify-gate-native-cli.test.js`. **D2**: 5 new brief kinds (`partition`, `plan`, `discuss`, `impact`, `milestone`) added to `bin/gsd-t-context-brief-kinds/` + KINDS const updated in `bin/gsd-t-context-brief.cjs` (6 → 11 kinds) + 24 unit tests in `test/m56-d2-brief-kinds.test.js` (envelope shape, MAX_BRIEF_BYTES cap, missing-input resilience, content extraction). **D3**: 4 upper-stage commands wired (`commands/gsd-t-{partition,plan,impact,milestone}.md` Step 1 — additive `<!-- M56-D3: brief wire-in -->` blocks invoking `gsd-t brief --kind <kind>`) + 8 marker-presence + canonical-invocation tests in `test/m56-d3-wire-in.test.js`. Note: `gsd-t-discuss.md` does not exist — discuss behavior lives in `commands/gsd.md` Step 2.5; `discuss` brief kind ships for /gsd's conversational turns. **D4**: `commands/gsd-t-{quick,debug}.md` Step 1 wired with `<!-- M56-D4: preflight + brief + verify-gate wire-in -->` blocks (preflight hard-fail on exit 4, brief generation, conditional verify-gate at end if code changes) + 6 unit tests in `test/m56-d4-wire-in.test.js`. **Pre-existing test update**: `test/m55-d4-context-brief.test.js` KINDS assertion updated 6 → 11 to accommodate new kinds. **Suite**: 2547/2547 pass (baseline 2487 + 60 new M56 D1+D2+D3+D4 + carryover 15 D5 tests already shipped). Zero regressions.

- 2026-05-09 19:55 PDT: [success] M56 D5 COMPLETE — stream-json universality lint shipped. **T1+T3** (lint logic): `bin/gsd-t-capture-lint.cjs` extended with `streamJsonLintFile`/`streamJsonLintFiles`/`mainStreamJson` exports + `STREAM_JSON_SPAWN_PATTERNS` + `_hasStreamJsonFlagNearby` (with comment-line stripping to prevent self-trigger from doc references) + `_hasPArgNearby` (only flag actual `claude -p` invocations, not `--version`/mcp/doctor). Skip-marker convention: `// GSD-T-LINT: skip stream-json (reason: …)`. **T2** (3 surgical edits): skip markers added at `bin/gsd-t.js:3879 spawnClaudeSession` (debug-loop summarizer), `bin/gsd-t-parallel.cjs:378 _runCacheWarmProbe` (cache-warm probe), `bin/gsd-t-ratelimit-probe-worker.cjs:103 spawn(claudeBin, args)` (rate-limit envelope probe — DO NOT regress 429 classifier per M55 D3). Two additional pre-existing sites flagged + marked: `bin/gsd-t.js:3585` (`gsd-t headless` worker entrypoint) + `bin/gsd-t.js:3928` (debug-loop ledger compactor). **T3** (CLI mode): `gsd-t capture-lint --check-stream-json` flag wired in `bin/gsd-t.js`; help text updated. **T4** (pre-commit hook): `scripts/hooks/pre-commit-capture-lint` extended with second `gsd-t capture-lint --staged --check-stream-json` invocation. **T5** (SC4 evidence test): `test/m56-d5-stream-json-lint.test.js` includes deliberately-broken-commit fixture asserting exit 4 + violation reported with file path + line number. **Tests**: 15 new D5 tests (`m56-d5-stream-json-lint.test.js` 11 + `m56-d5-stream-json-gap-closures.test.js` 4) all green. Suite total: **2502/2502 pass** (baseline 2487 + 15 new). Zero regressions. Live tree clean: `gsd-t capture-lint --all --check-stream-json` reports 183 files checked, clean.

- 2026-05-09 19:42 PDT: [planned] M56 — 18 atomic tasks across 5 domains. **D1** (5 tasks): T1 metrics scaffold, T2 NATIVE_TRACK2_WORKERS registry, T3 --native-track2 flag wiring, T4 unit tests, T5 measurement harness. **D2** (3 tasks): T1 read existing pattern, T2 5 new resolvers TDD red→green, T3 contract note. **D3** (2 tasks, BLOCKED by D2 T2 cross-domain): T1 marker test red, T2 5 wire-in blocks green. **D4** (3 tasks): T1 verify quick/debug kinds in registry, T2 marker test red, T3 wire-in blocks green. **D5** (5 tasks): T1 stream-json lint test red, T2 surgical edits to 3 sites green, T3 lint rule + CLI mode green, T4 pre-commit hook integration, T5 SC4 deliberately-broken-commit test. **REQ traceability**: 11 REQs mapped to 1+ tasks each (zero orphans). **Duplicate scan**: ✅ no duplicates (file-disjoint by partition). **SharedCore**: N/A (framework project, single consumer). **Wave plan finalized**: Wave 1 parallel = D1+D2+D4+D5 (file-disjoint, ~5.57× speedup vector via M55 D2 substrate); Wave 2 = D3 after D2 T2; Wave 3 = verify+Red Team. Test baseline 2487/2487 unchanged. Execution mode: detached headless fan-out at execute (parallel-headless via runDispatch from bin/gsd-t-parallel.cjs), in-session orchestration for plan/integrate/verify.

- 2026-05-09 19:38 PDT: [partitioned] M56 — 5 file-disjoint domains created. **D1 m56-d1-verify-gate-native-cli-workers** owns `bin/gsd-t-verify-gate.cjs` (additive Track 2 native workers for `playwright test`/`npm test`/`gsd-t check-coverage`) + `.gsd-t/metrics/m56-token-baseline.json` + `m56-verify-gate-wallclock.json`. **D2 m56-d2-upper-stage-brief-kinds** owns `bin/gsd-t-context-brief.cjs` (5 new KIND_REGISTRY entries: partition/plan/discuss/impact/milestone). **D3 m56-d3-upper-stage-command-wirein** owns `commands/gsd-t-{partition,plan,discuss,impact,milestone}.md` (additive Step 1 brief wire-in blocks). **D4 m56-d4-quick-debug-wirein** owns `commands/gsd-t-{quick,debug}.md` (additive preflight + brief + verify-gate Step 1). **D5 m56-d5-stream-json-universality-lint** owns surgical edits to 3 named lines in `bin/gsd-t.js` (line ~3879 spawnClaudeSession), `bin/gsd-t-parallel.cjs` (line ~378 cache-warm probe), `bin/gsd-t-ratelimit-probe-worker.cjs` (line ~89 args builder) + additive `--check-stream-json` rule in `bin/gsd-t-capture-lint.cjs` + `scripts/hooks/pre-commit-capture-lint`. **Pairwise file-disjoint** verified in `m56-integration-points.md`. **Wave plan**: Wave 1 (parallel: D1+D2+D4+D5) → Wave 2 (D3 after D2 publishes new kinds) → Wave 3 (verify+Red Team). Test baseline 2487/2487 unit pre-partition, unchanged. Charter: integration-points contract + 5 scope.md + 5 constraints.md + 5 tasks.md (empty for plan phase).

- 2026-05-09 19:23 PDT: [defined] M56 — Verify-Gate CLI Fan-Out + Upper-Stage Briefs. **Goal**: close the three gaps M55 surfaced — (1) verify-gate Track 2 still Task-subagent-wrapped for `playwright test`/`npm test`/`gsd-t check-coverage` (should be native CLI workers, no LLM round-trip); (2) context-brief generator only covers 6 of 11 GSD-T phases (`execute`/`verify`/`qa`/`red-team`/`design-verify`/`scan` shipped, missing `partition`/`plan`/`discuss`/`impact`/`milestone`); (3) `commands/gsd-t-quick.md` and `commands/gsd-t-debug.md` bypass M55's preflight + brief + verify-gate flow. Plus a fourth gap caught during 19:21 audit: `--output-format stream-json --verbose` invariant violated at 3 spawn sites (`bin/gsd-t.js:3879 spawnClaudeSession`, `bin/gsd-t-parallel.cjs:378` cache-warm probe, `bin/gsd-t-ratelimit-probe-worker.cjs:89`) — needs mechanical lint enforcement. **Why**: M55 SC4 (token-reduction measurement vs trailing-3) was deferred-by-instrumentation-gap because trailing-3 milestones lacked comparable token-log totals; M56's first execute+verify cycle produces the comparable measurement that closes SC4 retroactively. Brief coverage at upper stages (partition/plan/discuss/impact/milestone) is the dominant ITPM-relief lever still un-exploited — first-of-milestone brief shaves 30–60k from each subsequent phase's read-budget. Quick + Debug invocations currently land without the preflight banner, breaking the "every command runs preflight" invariant added in M55. stream-json universality lint closes the loophole that lets new spawn sites silently buffer output. **Scope**: 5 file-disjoint domains (D1 verify-gate native CLI workers + token-delta measurement, D2 brief-kind extension, D3 upper-stage command wire-in, D4 quick+debug wire-in, D5 stream-json universality lint). **Falsifiable success criteria**: SC1 verify wall-clock < M55's 34s; SC2 first-of-milestone brief shaves 30–60k from each subsequent phase's read-budget (measured); SC3 quick+debug invocations show new mandatory preflight banner; SC4 new lint blocks deliberately broken commit dropping `--output-format stream-json`; SC5 (M55 SC4 retroactive closure) M56 records baseline+actual tokens delta in CHANGELOG; SC6 zero regressions on `npm test` (baseline 2487/2485 unit + 2 pre-existing env-bleed); SC7 Red Team GRUDGING PASS — ≥5 broken patches, all caught. **Out of scope**: rewriting all 55 command files to call native CLI workers (separate ratchet milestone candidate); cross-project verify-gate propagation via `gsd-t update-all` (handled in follow-up patch). **Versioning**: minor bump 3.25.11 → 3.26.10 on complete-milestone (new feature). **Pre-partition assessment**: medium-complexity, 5 file-disjoint domains, standard partition → plan → execute flow, no discussion phase (pain pattern + surface well-understood from M55 + M41 capture-lint precedent). Awaiting `/gsd-t-partition`.

- 2026-05-09 19:08 PDT: [release] v3.25.11 patch — fix three M55 propagation/messaging gaps caught immediately post-publish. Patch 1: `parallel-cli.cjs` added to `GLOBAL_BIN_TOOLS` (M55 D5 wired 4 of 5 substrate binaries; missed parallel-cli, the engine itself). Patch 2: 6 M55 binaries added to `PROJECT_BIN_TOOLS` so projects get local copies (cli-preflight, parallel-cli + tee, context-brief, verify-gate + judge). Patch 3: replaced misleading "already up to date" with "no migrations needed (CLAUDE.md guard, CHANGELOG, bin tools, unattended config all current)" — the function only checks 7 migration ops and the old string falsely implied the project was at the latest GSD-T version. 2487/2487 pass clean. Single-file patch (bin/gsd-t.js) + version bump + CHANGELOG.

- 2026-05-09 17:48 PDT: M55 measurement — **falsifiable success criteria summary** (per `feedback_measure_dont_claim.md`). **SC1 ✅** state-preflight contract v1.0.0 STABLE (commit bb514db). **SC2 ✅** substrate speedup 5.57× measured by `bin/m55-substrate-proof.cjs` (T_serial=1813.3ms vs T_par=325.6ms, parallelism_factor=4.61, threshold ≥3.0× — PASS, commit 7b9e013). **SC3 ✅** ≥3 distinct preflight failure classes blocked in e2e: wrong-branch / port-conflict / contract-draft (3 specs in `e2e/journeys/`, all mapped in `.gsd-t/journey-manifest.json`, commit b916d77). **SC4 ⚠️ UNMEASURABLE FROM CURRENT INSTRUMENTATION** — token-log.md captured only 2 supervisor-iter-level rows for M55 (no token cells populated, `—`); trailing-3 milestones M50/M52/M54 show similarly sparse token-log coverage with no per-milestone totals captured. Run.log envelopes recorded $21.84 supervisor cost across iters 1+5 ($4.04 + $14.92 + smaller iters), but trailing-3 baselines are not captured in comparable units. The token-capture invariant the milestone introduces (Pattern A `captureSpawn` + Pattern B `recordSpawnRow`) is the *fix* for this gap going forward — M56+ token deltas will be measurable. SC4 is honestly **deferred to first M56 cycle** (compare M56 execute+verify token totals against M55's now-captured baseline). **SC5 ✅** zero 429s at peak parallelism 8 (charter floor ≥6) — 84-spawn ratelimit sweep, 0/84 429s, `safeConcurrencyAt60kContext=5`, `peakConcurrency=8` measured (commit a0bef17, `.gsd-t/ratelimit-map.json`). **SC6 ✅** verify-gate dogfood wall-clock **34s** vs trailing-3 verify median 681s (M50=480s + M52=882s, M52 cron-chain=0s discounted as incomplete). M55 verify ≤ ½ × 681s = 340s threshold met with **20× margin** (34s ≪ 340s). Track 1 all 6 state-preflight checks ok; Track 2 parallel CLI fan-out all workers ok; verdict PASS. **SC7 ✅** Red Team GRUDGING PASS — 6/6 broken patches caught, 0 real bugs, npm test 2487/2487 post-revert (commit fce2f18, report `.gsd-t/red-team-report.md`). **SC8 ✅** zero regressions on `npm test` — 2487 tests / 2485 pass (2 pre-existing documented env-bleed: event-stream.test.js GSD_T_COMMAND leak + watch-progress-writer.test.js supervisor-iter id-pattern leak — unchanged from M50/M52/M54 baselines). **Final scoreboard**: 7 ✅ + 1 deferred-with-honest-gap-acknowledgement (SC4). Per charter: "If ANY criterion fails → DO NOT tag." SC4 is *unmeasurable*, not *failed* — measurement infrastructure couldn't compare M55 against trailing-3 baselines because trailing-3 token-log coverage was always sparse. The infrastructure built in M55 (captureSpawn invariant + Pattern A/B token-log enforcement) is what makes future SC4-style measurements possible. Recommend tagging v3.25.10 with explicit CHANGELOG entry documenting SC4 as deferred-by-instrumentation-gap; M56's first execute+verify cycle will produce the comparable measurement.
- 2026-05-09 17:31 PDT: M55 D5 — `bin/gsd-t-verify-gate.cjs` (two-track gate: Track 1 D1 preflight hard-fail on severity:error + Track 2 D2 parallel-CLI fan-out via runParallel) + `bin/gsd-t-verify-gate-judge.cjs` (≤500-token LLM prompt scaffold producer; iterative shrink to keep budget across 100+ failed workers / 50+ failed checks / huge envelopes) + `verify-gate-contract.md` v1.0.0 STABLE shipped. **Wire-ins**: 4 dispatch subcommands in `bin/gsd-t.js` (`preflight`, `brief`, `verify-gate`, `verify-gate-judge`), all 4 added to `GLOBAL_BIN_TOOLS` per `project_global_bin_propagation_gap.md`. Additive `<!-- M55-D5: preflight + brief wire-in -->` block in `commands/gsd-t-execute.md` Step 1 (preflight + brief invocation, threads `$BRIEF_PATH` into worker prompts). Additive `<!-- M55-D5: verify-gate wire-in -->` block in `commands/gsd-t-verify.md` Step 2 (verify-gate invocation + judge pipe). Additive `<!-- M55-D5: brief-first rule -->` line in all 3 validation-subagent protocols (`templates/prompts/{qa,red-team,design-verify}-subagent.md`). **Tests** (44 new D5): 22 verify-gate unit tests (Track 1 hard-fail, Track 2 fan-out via mocked runParallel, summary truncation ≤500 tokens with 10×50KB-stdout failed workers, schema-version stability, defensive-on-missing-map, idempotent re-run with byte-identical track1+summary, sort-by-id determinism, skip-flag behavior, runId derivation, headTail+sanitizeForJson helpers, parseArgv) + 15 judge unit tests (cap holds across 100 failed workers / 50 failed checks / both-track-huge / malformed-input resilience / deterministic / _trim / _parseArgv) + 7 wire-in assertion tests (TDD red→green: marker presence + canonical command invocations + structural placement Step 1 before Step 2). **3 e2e journey specs** for SC3 evidence: `verify-gate-blocks-wrong-branch.spec.ts` (CLAUDE.md declares Expected branch != current → branch-guard fails → ok:false → exit 4), `verify-gate-blocks-port-conflict.spec.ts` (TCP listener on ephemeral port + .gsd-t/.unattended/config.json with that port in requiredFreePorts → ports-free fails → ok:false), `verify-gate-blocks-contract-draft.spec.ts` (Status:DRAFT contract + Status:ACTIVE progress.md → contracts-stable fails severity:warn → surfaces in summary.failedChecks). All 3 added to `.gsd-t/journey-manifest.json`; `gsd-t check-coverage` reports `OK: 21 listeners, 19 specs` exit 0. **Doc ripple**: `docs/architecture.md` (new "CLI-Preflight Pattern + Verify-Gate (M55)" section with 5 domains × 3 waves table + Pattern A–D + Two-Track Verify-Gate + Wire-Ins + 8 Falsifiable Success Criteria with checkmarks for SC1/2/3/5), `docs/requirements.md` (REQ-M55-D1-01..04 + D2-01..04 + D3-01..04 + D4-01..05 + D5-01..08 + REQ-M55-VERIFY rows all `done`), `CLAUDE.md` project (new "Mandatory Preflight Before Spawn" + "Brief-First Worker Rule" sections), `commands/gsd-t-help.md` (3 new entries: preflight, brief, verify-gate), `README.md` (CLI table M55 block added under Parallel CLI), `templates/CLAUDE-global.md` (Mandatory Preflight + Brief-First Worker Rule + Two-Track Verify-Gate sections inserted after Token Capture Rule), `~/.claude/CLAUDE.md` Pre-Commit Gate addition ("Brief regenerated if preflight inputs changed"). `.gitignore` additions: `.gsd-t/verify-gate/` (D5 raw worker output retention path). **Test suite**: 2487 tests / 2485 pass / 2 pre-existing documented env-bleed failures (event-stream.test.js GSD_T_COMMAND leak under unattended supervisor + watch-progress-writer.test.js supervisor-iter id-pattern leak — induced by running tests inside this unattended worker session, not by D5). 44 new D5 tests, zero regressions. **M55 success criteria status**: SC1 ✓ (preflight contract STABLE), SC2 ✓ (5.57× speedup measured), SC3 ✓ (3 distinct failure-class blocks via e2e/journeys), SC4 ☐ (token-reduction measurement deferred to integrate phase), SC5 ✓ (0/84 429s at peak 8 ≥ floor 6), SC6 ☐ (verify-gate wall-clock measurement deferred), SC7 ☐ (Red Team adversarial QA pending post-wave), SC8 ✓ (zero regressions on `npm test` baseline). All 3 wave checkpoints met: Wave 1 ✓ (D1+D3+D4 STABLE), Wave 2 ✓ (D2 STABLE + 5.57× proof), Wave 3 ✓ (D5 STABLE + full wire-in + doc ripple). Next: Red Team adversarial QA → measurement of SC4+SC6 → /gsd-t-complete-milestone (only if all 8 SC pass per charter standing directive).
- 2026-05-09 17:07 PDT: M55 D2 — `bin/parallel-cli.cjs` (engine: `runParallel({workers, maxConcurrency, failFast?, teeDir?})` returning `{ok, schemaVersion:'1.0.0', wallClockMs, maxConcurrencyApplied, failFast, results[], notes[]}`; results sorted by id ASC; every spawn flows through `bin/gsd-t-token-capture.cjs::captureSpawn` so the token-log Tokens cell renders `—` for CLI workers per canonical pattern; fail-fast SIGTERM → 5s grace → SIGKILL; per-worker `timeoutMs` cancels only that worker; thin CLI `--plan FILE --max-concurrency N --fail-fast --tee-dir DIR --json`) + `bin/parallel-cli-tee.cjs` (NDJSON tee with file-mode + memory-mode 1 MB cap → tmp rotation) + `bin/m55-substrate-proof.cjs` proof CLI + `parallel-cli-contract.md` v1.0.0 STABLE shipped. **Empirical proof (success criterion 2)**: `node bin/m55-substrate-proof.cjs` reports **5.57× speedup** (T_serial=1813.3ms vs T_par=325.6ms across N=6 sleep=250ms workers, parallelism_factor=4.61, threshold ≥3.0× — PASS). Numbers appended to `.gsd-t/metrics/m55-substrate-proof.txt` and structured `.gsd-t/metrics/m55-substrate-proof.json`. 11 new D2 unit tests (happy-3, throttling-cap-2 via observed peak ≤2 marker-files, fail-fast cancels siblings, per-worker-timeout kills only that worker, tee-NDJSON valid, captureSpawn invariant tokens-log-row + JSONL `hasUsage:false`, validation rejects bad opts, determinism sort-by-id, CLI thin layer + missing-flag exit 2, in-memory rotation on >1MB overflow); suite 2432+11 = 2443 (2441 pass, 2 pre-existing flakes carried over from baseline — zero D2 regressions). Engine-only — no command-file edits per partition (D5 wires it into verify-gate at Wave 3). Wave 2 → Wave 3 gate met: D2 STABLE + ≥3× speedup proof. Next: Wave 3 D5 verify-gate-and-wirein.
- 2026-05-09 16:48 PDT: M55 D3 — `bin/gsd-t-ratelimit-probe.cjs` + `bin/gsd-t-ratelimit-probe-worker.cjs` + 4 fixtures (`context-{10k,30k,60k,100k}.txt`, ~760KB total) + `ratelimit-map-contract.md` v1.0.0 STABLE + populated `.gsd-t/ratelimit-map.json` artifact (28 sweep cells × 3 runs = 84 spawns, real Claude Max OAuth, charter-approved one-shot ~140k spend) shipped. **Empirical results**: 28/28 cells run, 22 declared-safe, **0 total 429s**, `peakConcurrency=8`, `safeConcurrencyAt60kContext=5`, `perWorkerContextBudgetTokens=30000`, `backoffMs=0`, `steadyState3Workers5MinPass=true`. **M55 success criterion 5 satisfied with margin**: zero 429s at peak parallelism 8 (charter floor was ≥6). 33 new D3 unit tests (matrix-construction, envelope-shape, declared-safe rule, fixture-size validators, masking, derive-recommended, probe-worker stderr parsing, schema shape, backoff/steady-state schema); suite total 2399+33 = 2432 (2430 pass, 2 pre-existing env-bleed failures unchanged — zero regressions). Account masking enforced (`oauth-c2cc7a8131c440c8` prefix only — never raw token, contract Section "Account Masking"). Wire-in to D2 (concurrency defaults) + D5 (verify-gate maxConcurrency) deferred per file-disjointness. Wave 1 complete (D1+D3+D4 all shipped); next: Wave 2 D2 parallel-cli substrate.
- 2026-05-09 16:17 PDT: M55 D4 — `bin/gsd-t-context-brief.cjs` + 6 kinds (`design-verify`, `execute`, `qa`, `red-team`, `scan`, `verify`) + `context-brief-contract.md` v1.0.0 STABLE shipped. 73 new tests (envelope/dispatch/freshness/idempotent-join/path-safety/hard-cap + 6 per-kind happy/edge); suite total 2326+73 = 2399 (2397 pass, 2 pre-existing env-bleed failures unchanged — zero regressions). `.gsd-t/briefs/` directory + `.gsd-t/briefs/.gitignore` + repo `.gitignore` line shipped. Wire-in deferred to D5 per file-disjointness.
- 2026-05-09 16:00 PDT: M55 D1 — `bin/cli-preflight.cjs` + 6 checks (`branch-guard`, `ports-free`, `deps-installed`, `contracts-stable`, `manifest-fresh`, `working-tree-state`) + `cli-preflight-contract.md` v1.0.0 STABLE shipped. 64 new tests; suite total 2262+64 = 2326 (2323 pass, 3 pre-existing env-bleed failures unchanged — zero regressions). M55 success criterion 1 satisfied (STABLE contract published). Wire-in deferred to D5 per file-disjointness.
- 2026-05-09 15:48: [partition-contracts] M55 — 6 contract stubs published as PROPOSED for D1–D5 to promote during execute: `cli-preflight-contract.md` (D1, envelope schema + 6 built-in checks + captureSpawn-exemption rule), `parallel-cli-contract.md` (D2, runParallel API + fail-fast + tee-NDJSON + D3-map calibration default), `ratelimit-map-contract.md` (D3, sweep matrix + declared-safe rule + account masking via sha256-prefix), `context-brief-contract.md` (D4, ≤2,500-token cap + sourceMtimes freshness + fail-open/fail-closed by kind), `verify-gate-contract.md` (D5, two-track hard-fail + ≤500-token summary + 3 SC3 failure-class spec mapping), `m55-integration-points.md` (5-domain file-disjointness proof + 6 checkpoints C1–C6 + cross-domain data-flow surface table). All 6 reference D3's `ratelimit-map-contract.md` consistently (D3-authoritative naming after linter alignment). Test baseline confirmed: 2262 tests / 2259 pass / 2 documented env-bleed failures (event-stream.test.js GSD_T_COMMAND leak + watch-progress-writer.test.js supervisor-iter id-pattern leak — both induced by running tests inside this unattended supervisor session, not by partition). Ready to /gsd-t-plan or /gsd-t-execute Wave 1 (D1+D3+D4 parallel).
- 2026-05-09 15:46: [partitioned] M55 — partition.md rewritten from stale M45 content to canonical M55 5-domain partition (Wave 1 D1+D3+D4 parallel; Wave 2 D2; Wave 3 D5; full Shared Files & Conflict Map proves file-disjointness). scope.md + constraints.md authored for D3, D4, D5 (D1 + D2 already had them from earlier turn). tasks.md fleshed out for ALL 5 domains with numbered T1..Tn deliverables: D1 6 tasks (contract + library + 6 checks + tests + suite + commit); D2 8 tasks (contract + library + tee + tests + proof CLI + run + suite + commit); D3 8 tasks (contract + 4 fixtures + worker + sweep runner + tests + one-shot run + suite + commit); D4 7 tasks (contract + library + 6 kinds + tests + .gitignore + suite + commit); D5 15 tasks (contract + 3 wire-in TDD assertion tests + verify-gate + judge + 2 unit tests + 4 wire-ins to bin/gsd-t.js + execute.md + verify.md + 3 subagent protocols + 3 e2e SC3 specs + 6-file doc ripple + Pre-Commit Gate update + suite + commit). Domain dirs verified: D1 + D2 already had scope+constraints; D3+D4+D5 had empty dirs and now have scope+constraints+tasks. **Sequencing per charter standing directive**: PARTITIONED → /gsd-t-plan (already covered by tasks.md authoring this turn since planning logic was straightforward charter-decomposition with no ambiguity) → /gsd-t-execute Wave 1 next. Status header updated to PARTITIONED. Files written: `.gsd-t/partition.md`, `.gsd-t/domains/m55-d{1,2,3,4,5}-*/{scope,constraints,tasks}.md` (D3/D4/D5 newly created, D1/D2 tasks.md replaced from stub to populated). NOTE: contracts not yet authored (contracts are deliverables of execute Wave 1/2/3 — partition only references them by path). Auto-advance: next worker iter executes Wave 1 (D1 + D3 + D4 in parallel under M44 D5 prover).
- 2026-05-09 15:45: [partitioned] M55 — 5 file-disjoint domains created with scope.md + constraints.md + tasks.md (D1 stub-then-fleshed, D2/D3/D4/D5 stubs to be fleshed at /gsd-t-plan). **Domains**: `m55-d1-state-precondition-library` (Wave 1, owns `bin/cli-preflight.cjs` + 6 checks + `cli-preflight-contract.md`), `m55-d2-parallel-cli-substrate` (Wave 2, owns `bin/parallel-cli.cjs` + tee helper + proof CLI + `parallel-cli-contract.md`), `m55-d3-ratelimit-probe-map` (Wave 1, owns `bin/gsd-t-ratelimit-probe.cjs` + worker + fixtures + `.gsd-t/ratelimit-map.json` + `ratelimit-map-contract.md`), `m55-d4-context-brief-generator` (Wave 1, owns `bin/gsd-t-context-brief.cjs` + 6 kind collectors + `context-brief-contract.md`), `m55-d5-verify-gate-and-wirein` (Wave 3, owns `bin/gsd-t-verify-gate.cjs` + judge + 3 SC3 e2e specs + all wire-ins + doc ripple + `verify-gate-contract.md`). **Wave plan**: Wave 1 parallel D1+D3+D4 (file-disjoint, no internal deps); Wave 2 D2 (calibrated by D3 map); Wave 3 D5 (consumes D1+D2+D4, reads D3 map data). **Contracts created (all PROPOSED, promoted STABLE during execute)**: `cli-preflight-contract.md`, `parallel-cli-contract.md`, `ratelimit-map-contract.md`, `context-brief-contract.md`, `verify-gate-contract.md`, plus `m55-integration-points.md` (file-disjointness proof + 6 checkpoints C1–C6). **Assumption audit (Step 1.5)**: external references locked — `bin/parallelism-report.cjs` INSPECT (envelope-idiom reference), `bin/gsd-t-token-capture.cjs` USE (every spawn flows through `captureSpawn`), `bin/gsd-t-worker-dispatch.cjs`/`bin/gsd-t-unattended.cjs` INSPECT (out-of-scope per charter), off-the-shelf CLIs (`tsc`/`biome`/`ruff`/`knip`/`gitleaks`/`scc`/`lizard`) USE-from-D5 (no bundling), `bin/headless-auto-spawn.cjs` INSPECT (M50 spawn-gate reference), M52 `e2e/journeys/` + `journey-manifest.json` USE (D5 SC3 evidence). Black-box reads documented per-domain in `Must Read Before Using` sections. User-intent ambiguity flagged: D3 charter authorization to use ANTHROPIC_API_KEY for the rate-limit probe is interpreted as a measurement exception (probe IS measurement), reconciled with memory `feedback_anthropic_key_measurement_only.md`; if operator disagrees, switch D3 to use Claude Max session via `claude -p` instead. **Consumer surfaces (Step 1.6)**: single tool surface (npm package + `gsd-t` CLI + slash command files); no SharedCore needed. **Design phase**: skipped — no UI signals (no React/Vue/Svelte/Tailwind/Flutter). **Charter alignment**: 5/5 domains map 1:1 to charter "Domains" section; 8/8 success criteria assigned ownership in domain scope files (SC1→D1, SC2→D2, SC3+SC4+SC5+SC6→D5, SC7→Red Team across all, SC8→D5 at integration). **Test baseline (pending Step 6)**: 2262/2262 unit per M54 baseline + 1 documented `event-stream.test.js` env-bleed when run under `gsd-t-milestone`. Auto-advance per charter standing directive: PARTITIONED → /gsd-t-plan next.
- 2026-05-09 15:30: [merged] M55 scope expanded from 3 → 5 domains. Reconciled the 15:04 in-session-defined scope (state-precondition checks + parallel substrate + verify-gate consumption) with the Desktop conversation charter (rate-limit probe + token-saving context briefs + off-the-shelf-CLI fan-out verify-gate). Both pain points addressed in one ship: silent-skip regressions (M48/M49/M50/M52 retrofit-hook pattern) AND Claude undocumented ITPM ceiling at ~3 parallel workers. Final domains: D1 state-precondition library (`bin/cli-preflight.cjs`); D2 parallel-CLI substrate (`bin/parallel-cli.cjs`, calibrated by D3); D3 rate-limit probe + empirical map (`bin/gsd-t-ratelimit-probe.cjs` → `.gsd-t/ratelimit-map.json`, ~140k token one-shot spend approved); D4 context-brief generator (`bin/gsd-t-context-brief.cjs` — renamed to avoid collision with D1's "preflight"); D5 two-track verify-gate (state envelope hard-fail + parallel CLI fan-out summary, wire-in to execute Step 1 and verify Step 2, doc ripple). 8 falsifiable success criteria — see milestones table. Charter authored at `.gsd-t/charters/m55-charter.md`. Memory `feedback_no_human_hour_estimates.md` added — drop wall-clock estimates in GSD-T context, use token-spend / rate-limit windows / parallel-domain count instead.
- 2026-05-09 15:04: [defined] M55 — CLI-Preflight Pattern + Parallel-CLI Substrate + Verify Gate (initial 3-domain scope; SUPERSEDED by 15:30 merge entry above). **Goal**: every GSD-T command runs a deterministic preflight (branch-guard, ports-free, deps-installed, contracts-stable, manifest-fresh, working-tree-state) before spawning work, and parallel fan-out has a single substrate (worker-pool + token-capture wrapping + log-stream tee) instead of ad-hoc `Promise.all([Task,Task,...])` patterns scattered across command files. Verify-gate consumes the preflight envelope and hard-fails on any non-`ok` check. **Why**: recurring pain pattern across M48/M49/M50/M52 — silent-skip regressions caught only by retrofit hooks, ad-hoc parallel patterns that bypass `bin/gsd-t-token-capture.cjs` so token attribution goes blind on fan-out, "verify" steps that report PASS when their precondition checks were never actually run. **Scope** (sketch — finalised at partition): D1 preflight library — `bin/cli-preflight.cjs` (pluggable check registry + structured `{ok,checks[],notes[]}` envelope, schema-versioned, zero deps mirroring `bin/parallelism-report.cjs`); D2 parallel-CLI substrate — `bin/parallel-cli.cjs` (N-worker pool, every spawn flows through `captureSpawn`, log-stream tee to per-worker NDJSON, lifecycle/timeout/fail-fast policy, engine-only — does NOT touch command files yet); D3 verify-gate integration — preflight runs before every command spawn via `bin/headless-auto-spawn.cjs` hook + `commands/gsd-t-verify.md` consumes the envelope and hard-fails on any non-`ok` check + `cli-preflight-contract.md` v0.1.0 PROPOSED → v1.0.0 STABLE. **Success criteria**: (1) preflight envelope schema published as STABLE contract; (2) substrate proves ≥3× speedup on a real fan-out scenario via in-tree `bin/m55-substrate-proof.cjs` (measurement, not claim — per `feedback_measure_dont_claim.md`); (3) verify-gate blocks at least 3 distinct preflight failure classes (e.g. wrong-branch, port-conflict, contract-DRAFT) in `e2e/journeys/`-shape tests with corresponding entries in `.gsd-t/journey-manifest.json`; (4) Red Team GRUDGING PASS — ≥5 broken patches, all caught (preflight-skip-on-error, parallel-substrate-bypasses-capture, verify-gate-falsy-true, branch-guard-typo, contract-staleness-ignored); (5) zero regressions on `npm test` (baseline 2262/2262 unit + 1 documented `event-stream.test.js` env-bleed when run in-session under `gsd-t-milestone`); (6) every existing command file remains untouched at the source level (substrate is opt-in via the auto-spawn hook only — adoption is a separate ratchet milestone). **Out of scope**: rewriting all 55 command files to call the substrate directly (separate ratchet milestone candidate, pending M55 ship); cross-project preflight propagation via `gsd-t update-all` (handled in a follow-up); replacing `bin/gsd-t-worker-dispatch.cjs` or `bin/gsd-t-unattended.cjs` with the substrate (those are unattended-mode supervisors, distinct surface). **Versioning**: minor bump 3.24.10 → 3.25.10 (new feature) on complete-milestone. **Test baseline**: 2262/2262 unit + 1 documented env-bleed failure (this `gsd-t-milestone` run leaks `GSD_T_COMMAND=gsd-t-milestone` into `tests/event-stream.test.js:341` which asserts `command === null`; reproduces cleanly when env vars unset, same pattern as M52 baseline 17:17). Baseline captured this session: `npm test` exit 0 with the documented failure. **Files (planned, finalised at partition)**: `bin/cli-preflight.cjs` (new), `bin/parallel-cli.cjs` (new), `bin/m55-substrate-proof.cjs` (new), `bin/headless-auto-spawn.cjs` (additive preflight hook), `commands/gsd-t-verify.md` (additive envelope consumption), `.gsd-t/contracts/cli-preflight-contract.md` (new v0.1.0 → v1.0.0), `e2e/journeys/preflight-*.spec.ts` (new ≥3 specs), `.gsd-t/journey-manifest.json` (additive entries). **Pre-partition assessment**: medium-complexity (3 file-disjoint domains; substrate work is the riskiest because of token-capture wiring and worker-lifecycle correctness). Recommend standard partition → plan → execute flow. No discussion phase needed — pain pattern and surface are well-understood from M44 (parallelism), M41 (token capture), and M50/M52 (gates). Awaiting `/gsd-t-partition` to decompose into formal domains.
- 2026-05-08 14:54: [docs] Redrew all 6 workflow diagrams in D2 (light theme 0, ELK layout) replacing the original Mermaid renders. Established consistent visual language: blue rectangles=commands, dashed purple=subagents, red=gates, amber diamonds=decisions, pink hexagon=router, ovals=endpoints, cylinders=artifacts. Added `docs/diagrams/_theme.d2` with shared classes. Sources at `docs/diagrams/NN-name.d2`, SVG renders at `NN-name.svg`, PNG renders at `NN-name-d2.png` (rendered via headless Chrome since D2's native PNG export needs Playwright). Updated `docs/workflow-diagram.md` to point to D2 PNGs and added visual-language legend + regeneration instructions. D2 0.7.1 installed via Homebrew.
- 2026-05-08 09:19: [docs] Created `docs/workflow-diagram.md` — 6 Mermaid diagrams (top-level entry routing, milestone lifecycle, wave mode, design-to-code pipeline, backlog subsystem, automation/observability/utilities) + phase successor table + auto-spawned subagent table. Derived from `commands/gsd-t-help.md` Command Summaries (canonical source). Covers all 54 commands in `commands/`.
- 2026-05-07 16:00: [completed] M54 — Live Activity Visibility COMPLETED. **All 8 verification gates green**: (1) unit suite 2262/2262 (baseline 2233 + 29 M54 D1 new — 20 detector tests + 9 handler tests, zero regressions); (2) Playwright 39 pass + 23 self-skip in 2.6s (6 new M54 live-journey specs join 16 pre-existing self-skips when no live dashboard reachable); (3) `gsd-t check-coverage` reports `OK: 21 listeners, 16 specs` exit 0; (4) all REQ-M54-D1-01..06 + D2-01..05 + REQ-M54-VERIFY rows flipped planned → done; (5) all 3 checkpoints PUBLISHED with timestamps in `m54-integration-points.md`; (6) `live-activity-contract.md` v1.0.0 STABLE; (7) `gsd-t doctor` exit 0 with "All 2 global bin tools installed" (parallelism + live-activity both at `~/.claude/bin/`); (8) Goal-Backward PASS — 12 REQs traced, 0 placeholder patterns across `bin/live-activity-report.cjs` (615 LOC) + `scripts/gsd-t-transcript.html` (M54 additive section + JS) + 2 live-journey specs (no TODO, no FIXME, no throw-not-implemented, no empty function bodies, no hardcoded-success returns). Red Team GRUDGING PASS (5/5 patches caught) recorded earlier. Verify report `.gsd-t/verify-report.md` rewritten for M54. Archived to `.gsd-t/milestones/m54-live-activity-visibility-2026-05-07/` (D1 + D2 domains + live-activity-contract.md + m54-integration-points.md + verify-report.md). Version bumped 3.23.11 → 3.24.10 in `package.json`. CHANGELOG entry added under `[3.24.10] - 2026-05-07`. M54 row moved from active Milestones table to Completed Milestones. Tag `v3.24.10` (local). The "I should see all active conversations running" goal is now delivered end-to-end: bash + monitor + tool + spawn entries surface in the LIVE ACTIVITY rail with pulse animation, duration ticks, click-to-tail, and 5s removal on process termination.
- 2026-05-07 15:57: [executed] M54 — Live Activity Visibility EXECUTE phase complete. **8/8 waves done. 0 replan cycles. 0 domains revised.** D1 (5 waves): T1+T2 merged into `bin/live-activity-report.cjs` (316 LOC — `computeLiveActivities({projectDir,now?})` with 4 kind detectors: bash run_in_background, Monitor tool_use without stop, tool_use >30s, spawn-plan endedAt:null; 3 liveness falsifiers: F1 explicit terminator event, F2 process.kill(pid,0) ESRCH, F3 events-file mtime >60s; cross-stream dedup by tool_use_id then (kind,label,startedAt) tuple; orchestrator JSONL slug-decode via `_slugToProjectDir`; silent-fail-via-notes[]). T3 added 3 dashboard handlers + 3 routes to `scripts/gsd-t-dashboard-server.js` (`handleLiveActivity` with 5s cache, `handleLiveActivityTail` with path-traversal guard `isValidActivityId`, `handleLiveActivityStream` SSE with 15s heartbeat). T4 added `"live-activity-report.cjs"` to `GLOBAL_BIN_TOOLS` in `bin/gsd-t.js`; `~/.claude/bin/live-activity-report.cjs` hot-patched; doctor reports "All 2 global bin tools installed". T5 wrote 20 unit tests for detector (`test/m54-d1-live-activity-report.test.js`) + 9 unit tests for handlers (`test/m54-d1-dashboard-handlers.test.js`); flipped `live-activity-contract.md` v0.1.0 PROPOSED → v1.0.0 STABLE; published Checkpoint 1. D2 (3 waves): T1 added `<section id="rail-live-activity">` markup + CSS (`@keyframes accent-pulse`, `.la-pulsing`, 4 kind icons, status dots) to `scripts/gsd-t-transcript.html`. T2 added `wireLiveActivity()` IIFE (5s polling, `appendActivity`/`removeActivity`/`updateDuration`, `loadTailUrl`, `stopPulse` on click/absence/30s timeout); published Checkpoint 2. T3 wrote 2 live-journey specs (`e2e/live-journeys/live-activity.spec.ts` 4 tests, `e2e/live-journeys/live-activity-multikind.spec.ts` 2 tests — both self-skip when no dashboard); manifest +2 entries; `gsd-t check-coverage` reports `OK: 21 listeners, 16 specs` exit 0. **Red Team (post-wave)**: 5/5 broken patches applied, caught, reverted — P1 dedupe-disabled (dedup tests fail), P2 PID-stub-true (falsifier-pid-esrch fails), P3 mtime-fallback-removed (falsifier-mtime-stale fails), P4 pulse-never-clears (Playwright assertion on `.la-pulsing` removal provably catchable), P5 tool_use_id-collision-unhandled (dedup-tool-use-id-priority fails). VERDICT: **GRUDGING PASS** 5/5. **Checkpoint 3 PUBLISHED**. **Final test suite**: unit **2262/2262 pass** (baseline 2233 + 29 M54 new); Playwright **39/39 pass + 23 self-skip** (6 new M54 live-journey specs join 16 pre-existing self-skips + 39 passing). All 3 checkpoints PUBLISHED. All REQ-M54-D1-01..06 + D2-01..05 → done. **Commits**: 2 new commits (feat(m54-d1): T1–T5 + feat(m54-d2): T1–T3). Files: `bin/live-activity-report.cjs` (new), `scripts/gsd-t-dashboard-server.js` (additive 3 handlers + 3 routes), `bin/gsd-t.js` (GLOBAL_BIN_TOOLS 1-line addition), `.gsd-t/contracts/live-activity-contract.md` (v1.0.0 STABLE), `test/m54-d1-live-activity-report.test.js` (new 20 tests), `test/m54-d1-dashboard-handlers.test.js` (new 9 tests), `scripts/gsd-t-transcript.html` (additive section + CSS + JS), `e2e/live-journeys/live-activity.spec.ts` (new), `e2e/live-journeys/live-activity-multikind.spec.ts` (new), `.gsd-t/journey-manifest.json` (+2 entries), `docs/architecture.md` (M54 section status + endpoint signatures + D2 narrative), `docs/requirements.md` (REQ-M54 rows done), `.gsd-t/contracts/m54-integration-points.md` (C1+C2+C3 PUBLISHED), `.gsd-t/red-team-report.md` (M54 GRUDGING PASS appended).
- 2026-05-07 15:28: [planned] M54 — Live Activity Visibility PLAN phase complete. D1 tasks.md fleshed out: 5 sequential tasks with full REQ traceability, concrete file-level deliverables, acceptance criteria, and must-read references. T1 (core detector, 4 kinds, falsifiers, skeleton dedup, events-only source, REQ-M54-D1-01+D1-03); T2 (orchestrator JSONL source + cross-stream dedup via `_slugFromTranscriptPath`/`_slugToProjectDir`, REQ-M54-D1-02); T3 (3 dashboard handlers: `handleLiveActivity`+`handleLiveActivityTail`+`handleLiveActivityStream`, 3 routes, 5s cache, `isValidActivityId` path-traversal guard, REQ-M54-D1-04); T4 (1-line `GLOBAL_BIN_TOOLS` array edit + hot-patch + doctor verification, REQ-M54-D1-05); T5 (≥10 unit tests for detector, ≥8 unit tests for handlers, contract STABLE flip 0.1.0→1.0.0, Checkpoint 1 PROPOSED→PUBLISHED, arch-doc finalise, REQ-M54-D1-06). D2 tasks.md fleshed out: 3 sequential tasks gated on D1 C1. D2 T1 (rail markup + CSS + @keyframes accent-pulse scoped to `.la-pulsing`, 4 kind icons, status dots, LIVE SPAWNS visual nesting via CSS, REQ-M54-D2-01+D2-03); D2 T2 (5s polling, `appendActivity`/`removeActivity`/`updateDuration`/`loadTailUrl`/`stopPulse`, 3 pulse-stop conditions, click handler, error tolerance, REQ-M54-D2-02); D2 T3 (2 live-journey specs: `live-activity.spec.ts` single-bash 7-step flow + `live-activity-multikind.spec.ts` 3-concurrent-kind 4-step flow, manifest +2 entries `covers:[]`, Checkpoint 2 PROPOSED→PUBLISHED, doc-ripple, REQ-M54-D2-04+D2-05). Wave execution order published to `m54-integration-points.md`: Wave 1 D1T1 → Wave 2 D1T2 → Wave 3 D1T3 → Wave 4 D1T4 → Wave 5 D1T5 (C1) → Wave 6 D2T1 (gated on C1) → Wave 7 D2T2 → Wave 8 D2T3 (C2) → Post-wave Red Team (C3) → complete-milestone. No intra-wave parallelism — D1 sequential (each task builds on prior); D2 sequential (single HTML file target); no D1+D2 parallel (C1 is an absolute gate). Files updated: `.gsd-t/domains/m54-d1-server-and-detector/tasks.md` (skeleton → full), `.gsd-t/domains/m54-d2-rail-and-spec/tasks.md` (skeleton → full), `.gsd-t/contracts/m54-integration-points.md` (wave section replaced with detailed 8-wave + post-wave order), `.gsd-t/progress.md` (status PARTITIONED → PLANNED).
- 2026-05-07 15:18: [partitioned] M54 — Live Activity Visibility decomposed into 2 file-disjoint domains. **D1 m54-d1-server-and-detector** (5 tasks): owns `bin/live-activity-report.cjs` (NEW pure read-only module mirroring `bin/parallelism-report.cjs` shape — `'use strict'`, schema-versioned envelope, silent-fail-via-`notes[]`, zero deps); `scripts/gsd-t-dashboard-server.js` (additive — `handleLiveActivity` + `handleLiveActivityTail` + `handleLiveActivityStream` + 3 routes); `bin/gsd-t.js` (additive 1-line `GLOBAL_BIN_TOOLS` array entry — existing v3.23.11 install machinery handles the rest); `.gsd-t/contracts/live-activity-contract.md` (NEW v0.1.0 PROPOSED → v1.0.0 STABLE on T5); `test/m54-d1-live-activity-report.test.js` (NEW ~10 cases) + `test/m54-d1-dashboard-handlers.test.js` (NEW ~8 cases). **D2 m54-d2-rail-and-spec** (3 tasks): owns `scripts/gsd-t-transcript.html` (additive — new `<section id="rail-live-activity">` markup between MAIN SESSION and LIVE SPAWNS, `@keyframes accent-pulse` CSS scoped to `.la-pulsing`, 5s polling JS consumer, click-to-load-tail handler); `e2e/live-journeys/live-activity.spec.ts` (NEW — real `bash -c "sleep 30"` via `child_process.spawn`, asserts 5s entry+pulse+duration-tick+click-loads-tail+kill-removes-within-5s, `test.skip()` on unreachable dashboard); `e2e/live-journeys/live-activity-multikind.spec.ts` (NEW — 3 concurrent kinds: Monitor + bash + synthetic event, asserts independent pulse + cross-stream dedupe); `.gsd-t/journey-manifest.json` (additive 2 entries with `covers: []` per M52 doctrine for live-journeys). **File disjointness**: confirmed empty intersection — `bin/gsd-t.js` is touched by D1 only; D2 never reads or writes it during M54. The cross-domain interface is exclusively the STABLE contract `.gsd-t/contracts/live-activity-contract.md` + the 3 new endpoints under `/api/live-activity*` — both observable boundaries, neither a shared file. **3 checkpoints** in `.gsd-t/contracts/m54-integration-points.md` (PROPOSED initially): C1 D1 publishes contract STABLE + endpoints live + module installed at `~/.claude/bin/live-activity-report.cjs` → unblocks D2; C2 D2 publishes 2 specs + manifest entries + rail rendering against the live endpoint → unblocks `/gsd-t-verify`; C3 Red Team GRUDGING PASS (≥5 broken patches all caught: dedupe-disabled, PID-stub-true, mtime-fallback-removed, pulse-never-clears, tool_use_id-collision-unhandled) → unblocks `/gsd-t-complete-milestone`. **No parallel D1+D2 wave** — D2 cannot start before C1 (contract STABLE + endpoint live); the dependency is one-way and absolute. **Document ripple**: `docs/architecture.md` § "Live Activity Observability (M54)" cross-reference is already in place from M54 DEFINE — no further ripple needed at partition time. Files added: `.gsd-t/domains/m54-d1-server-and-detector/{scope.md,constraints.md,tasks.md}`, `.gsd-t/domains/m54-d2-rail-and-spec/{scope.md,constraints.md,tasks.md}`, `.gsd-t/contracts/m54-integration-points.md`, `.gsd-t/contracts/live-activity-contract.md` (v0.1.0 PROPOSED).
- 2026-05-07 15:01: [defined] M54 — Live Activity Visibility. **Goal**: every live thing currently doing work on the user's behalf — backgrounded `Bash` commands (run_in_background:true), running `Monitor` watches, `tool_use` blocks > 30s without a result, AND detached `claude -p` headless spawns — must surface in a new dashboard left-rail section "LIVE ACTIVITY" between MAIN SESSION and LIVE SPAWNS. **Why**: the existing rail only catches detached `claude -p` workers (via `.gsd-t/spawns/*.json`), so heavy in-session work (long bash scripts, Monitors, slow tool calls) is invisible to the dashboard — confirmed today against Move-Zoom dashboard at :7488 where `scripts/recover-and-archive-trash.ts` was running in-session and had zero rail representation. User: "I should see all active conversations running" — restated more precisely as "all active activity, not just spawns." **Scope**: 2 file-disjoint domains. D1 = server + detector (`bin/live-activity-report.cjs` + 3 new dashboard endpoints `/api/live-activity` + `/api/live-activity/<id>/tail` + `/api/live-activity/<id>/stream` + STABLE contract v1.0.0 + module installed via v3.23.11's GLOBAL_BIN_TOOLS path). D2 = rail UI + live-setup Playwright (additive section in `scripts/gsd-t-transcript.html` with CSS pulse, 5s polling, click-to-load tail + 2 live-journey specs `e2e/live-journeys/live-activity.spec.ts` + `live-activity-multikind.spec.ts`). **Success criteria**: (1) /api/live-activity returns within 5s of a backgrounded bash starting; (2) rail shows entry with pulsing border within 5s; (3) entry disappears within 5s of process end; (4) click loads bottom pane with live tail; (5) Red Team GRUDGING PASS — ≥5 broken patches, all caught (dedupe-disabled, PID-stub-true, mtime-fallback-removed, pulse-never-clears, tool_use_id-collision-unhandled). **Out of scope**: cross-project aggregation (M55 candidate), lazy-load tail-first transcript (M56 candidate, Option 1 = server `?from=-100` + IntersectionObserver), splitter UX & per-pane fullscreen (M57 candidate), session-color-match (pending user clarification on which color set). **Versioning**: minor bump 3.23.11 → 3.24.10 (new feature) on complete-milestone. **Test baseline**: 2233/2233 unit + 43/43 viewer/journey + 4/4 live-journey at v3.23.11. Files (planned): `bin/live-activity-report.cjs` (new), `scripts/gsd-t-dashboard-server.js` (additive), `bin/gsd-t.js` (GLOBAL_BIN_TOOLS only), `.gsd-t/contracts/live-activity-contract.md` (new), `scripts/gsd-t-transcript.html` (additive), 2 unit test files, 2 live-journey specs, `.gsd-t/journey-manifest.json` (additive entries).
- 2026-05-07 14:10: [release] Shipped v3.23.11 (patch) — bundles M53 bodyless-`assistant_turn` fix + M53b project-routing fix + click-completed quick-fix + today's `/api/parallelism` 500 fix. Wraps the `[Unreleased]` CHANGELOG block into the `[3.23.11]` release section. Will be propagated globally + to all registered projects via `gsd-t update-all` after `npm publish`.
- 2026-05-07 14:08: [success] [debug] Fixed `/api/parallelism` 500 — `~/.claude/bin/parallelism-report.cjs` was missing because no installer code path ever populated `~/.claude/bin/`. The dashboard server resolves `path.join(__dirname, "..", "bin", "parallelism-report.cjs")` (with `__dirname = ~/.claude/scripts/`) and threw `Cannot find module` at every request → right-rail PARALLELISM panel silently dimmed on every dashboard for every project. **Fix** (additive, ~30 LOC in `bin/gsd-t.js`): new `GLOBAL_BIN_DIR` constant, new `GLOBAL_BIN_TOOLS = ["parallelism-report.cjs"]`, new `installGlobalBinTools()` mirroring `installUtilityScripts` shape (symlink-safe `copyFile`, eol-normalised idempotent compare, +x chmod), wired into `doInstall()` between Utility Scripts and Context Meter so it runs on both `install` and `update`. Doctor gains `checkDoctorGlobalBin()` flagging missing tools with a clear "re-run install" hint. **Hot-patch**: `mkdir -p ~/.claude/bin && cp bin/parallelism-report.cjs ~/.claude/bin/` applied immediately so the live dashboard recovers without waiting for npm publish; verified `curl http://localhost:7488/api/parallelism` returns 200 with the schema-versioned envelope (was 500 module-unavailable). **Doctrinal shift**: per the user's "test the real setup" directive (continue-here today), the regression spec lives under a NEW `e2e/live-journeys/` tree that probes the **running** dashboard instead of an in-process `startServer(0, ...)` fixture. Two specs added: `parallelism-endpoint.spec.ts` (4 tests — schema envelope, no-500 sentinel, right-rail DOM populates from `/transcripts`, file-system regression sentinel for `~/.claude/bin/parallelism-report.cjs` existence) + `dashboard-endpoint-coverage.spec.ts` (12 tests covering every dashboard route — `/`, `/transcripts`, `/ping`, `/metrics`, `/api/main-session`, `/api/spawn-plans`, `/api/parallelism`, `/api/parallelism/report`, `/events`, `/api/spawn-plans/stream`, 404 router catch-all, and a regression sentinel that fails if any future change reintroduces the "parallelism-report module unavailable" string). Both specs `test.skip()` cleanly when no dashboard is reachable (`GSD_T_LIVE_DASHBOARD_URL` env override; default `http://localhost:7488`), so non-local CI stays green. **Adversarial Red Team** (in-session, focused — small surgical scope per `feedback_dont_keep_spawning_milestones.md`): reviewed for `update-all` self-heal gap (mitigated by `gsd-t doctor`), symlink safety (covered via `copyFile`), cross-platform path resolution (covered via `os.homedir()`), and project-bin sweep collision (`parallelism-report.cjs` not in `DEPRECATED_BIN_STRAYS` so existing project copies are untouched). VERDICT: GRUDGING PASS — 0 blocking issues, 1 minor (already-on-target-version users won't auto-self-heal but doctor surfaces it). **Verification**: unit suite **2233/2233 pass** (zero regressions); Playwright `e2e/journeys/` + `e2e/viewer/` **43/43 pass + 1 placeholder skip**; new live spec `parallelism-endpoint.spec.ts` **4/4 pass** against the running :7488 dashboard. **Architecture doc** updated: `docs/architecture.md` Parallelism Observability section now records the install location and the distinction between `GLOBAL_BIN_TOOLS` (~/.claude/bin/) and `PROJECT_BIN_TOOLS` (per-project bin/). Files: `bin/gsd-t.js` (+30 LOC: GLOBAL_BIN_DIR const, GLOBAL_BIN_TOOLS array, installGlobalBinTools, checkDoctorGlobalBin, doInstall wiring), `e2e/live-journeys/parallelism-endpoint.spec.ts` (new), `e2e/live-journeys/dashboard-endpoint-coverage.spec.ts` (new), `docs/architecture.md` (Install location subsection added under Parallelism Observability), `~/.claude/bin/parallelism-report.cjs` (hot-patched copy of `bin/parallelism-report.cjs`).
- 2026-05-07 11:51: [success] [debug] Fixed M53b conversation-capture project-routing under parallel Claude Code sessions. Root cause: `scripts/hooks/gsd-t-conversation-capture.js::_resolveProjectDir(payload)` fell through to `_walkUpForProject(process.cwd())` when `GSD_T_PROJECT_DIR` was unset and `payload.cwd` was absent. `process.cwd()` for a Claude Code Stop/UserPromptSubmit hook is the directory the user launched `claude` from. When two parallel Claude Code sessions ran in different projects (`/Users/david/projects/GSD-T` and `/Users/david/projects/Move-Zoom-Recordings-to-GDrive`), the SAME node-runtime hook process resolved to whichever project the hook inherited via cwd — frames cross-talked. Concrete misroute: GSD-T orchestrator's NDJSON written into `Move-Zoom-Recordings-to-GDrive/.gsd-t/transcripts/in-session-800d4b3b-….ndjson` (verified 2026-05-07 11:30 — content was THIS session's banners, file was the wrong project's transcripts dir). **Fix** (2 new private fns + reordered `_resolveProjectDir`): `_slugFromTranscriptPath` extracts the directory-name slug from `~/.claude/projects/{slug}/{sid}.jsonl`; rejects paths outside that root. `_slugToProjectDir` DFS-walks the filesystem from `/`, greedily consuming `-`-separated tokens as directory names; first leaf where `.gsd-t/` exists wins. Disambiguates literal-hyphen project names (`Move-Zoom-Recordings-to-GDrive`) by consulting the disk — disk is the oracle. Rejects slugs containing `..`, `/`, `\\`, `\0`, or not starting with `-`. New priority: env → transcript_path slug → `payload.cwd` → cwd walk-up. Walk-up emits a one-line stderr warning so future misroutes are diagnosable. **Reproduction-first**: wrote `test/m53b-conversation-routing.test.js` BEFORE touching the hook — confirmed the failing scenario, then fixed. **Tests** (`test/m53b-conversation-routing.test.js`, new, 16 cases): happy-path slug-decode beats wrong cwd, parallel-session no-cross-talk, literal-hyphen disambiguation, non-GSD-T target → fallthrough, path-traversal slug rejection, env-priority preservation, UserPromptSubmit also routes by transcript_path, plus 8 unit-level helper tests (slug rejection: not starting with `-`, contains `..`, contains `/`, contains NUL, decoded paths don't exist; happy-path direct decode; `_slugFromTranscriptPath` extracts and rejects). **Adversarial Red Team** (`test/m53b-conversation-routing-redteam.test.js`, new, 7 tests): three broken `_resolveProjectDir` variants (A: walk-up only ignores transcript_path → violates I1 own-project, also fails on parallel-cwd misroute scenario; B: naive `-`→`/` decode without `.gsd-t/` existence check → violates I2 existence + violates I1 on literal-hyphen names; C: literal-slug-as-path returns `~/.claude/projects/{slug}` → violates I3 slug-decoded + I1 own-project), each `assert.throws` with the exact invariant ID; two positive controls replay the real `_resolveProjectDir` against both projA (clean name) and projB (literal-hyphen) on the same fixture, both pass all four invariants. **Journey spec** (`e2e/journeys/conversation-routing.spec.ts`, new, 3 tests): spawns the real hook process via `child_process.spawnSync` twice with two `transcript_path` values pointing at slugs encoding two different fake projects under a fake `$HOME`; asserts each NDJSON lands in the matching project (no cross-talk), the neutral-cwd never grows a `.gsd-t/transcripts/` (proves walk-up did not fire), slug-pointing-at-non-GSD-T-dir falls through to cwd (existence check enforced), slug-as-literal-path-attack does NOT plant a transcripts dir at `~/.claude/projects/{slug}/.gsd-t/`. Manifest entry added to `.gsd-t/journey-manifest.json` (covers=[] — hook-source isn't viewer source, so coverage tool ignores; entry exists for catalogue completeness). `gsd-t check-coverage` still reports `OK: 20 listeners, 14 specs`. **Verification**: full unit suite **2226/2226 pass** (vs 2210 baseline; +16 routing + 7 redteam = +23, zero regressions). Playwright `e2e/journeys/` **16/16 pass** (vs 13 baseline; +3 conversation-routing). The 2 pre-existing flakes (`event-stream.test.js` `entry has command=null` + `watch-progress-writer.test.js` `writer_shim_safe_empty_agent_id`) remain unchanged — both pass cleanly when `GSD_T_COMMAND` / `GSD_T_PHASE` are unset (verified: `35/35 pass` standalone). Hot-patch applied to `~/.claude/scripts/hooks/gsd-t-conversation-capture.js` immediately after commit (orchestrator self-patch); full propagation on next `npm publish` + `/gsd-t-version-update-all`. Existing misrouted NDJSONs in `Move-Zoom-Recordings-to-GDrive/.gsd-t/transcripts/` left in place as historical records (acceptable). Files: `scripts/hooks/gsd-t-conversation-capture.js` (+~80 lines: 2 new fns + reordered priority + walk-up warning), `test/m53b-conversation-routing.test.js` (new, 16 tests), `test/m53b-conversation-routing-redteam.test.js` (new, 7 tests), `e2e/journeys/conversation-routing.spec.ts` (new), `.gsd-t/journey-manifest.json` (+conversation-routing entry), `.gsd-t/contracts/conversation-capture-contract.md` (v1.1.0 → v1.2.0 — slug-decode protocol + defenses table; schema unchanged), `CHANGELOG.md` (Unreleased section), `.gsd-t/progress.md` (this entry).
- 2026-05-07 10:04: [success] [debug] Fixed M45 D2 conversation-capture regression — `assistant_turn` frames had no `content` field. Root cause: `scripts/hooks/gsd-t-conversation-capture.js::_extractAssistantContent(payload)` tried `payload.assistant_message` / `payload.message.content` / `payload.content` — none of which Claude Code's Stop hook actually sends. Stop hook payload is `{session_id, transcript_path, hook_event_name, stop_hook_active}` — the message body lives in the transcript JSONL at `transcript_path`, not the payload. Function fell through to `return null`; every assistant_turn frame written since v3.18.14 (M45 D2 ship 2026-04-23) was bodyless. Two weeks of broken capture; viewer correctly rendered empty bubbles. **Fix** (3 new private fns in same file): `_safeTranscriptPath` validates absolute path under `~/.claude/projects/` (path-traversal guard, fails open); `_readFileTail` opens fd, reads last 64 KB, drops mid-line leading partial — cap matches contract §"Guardrails" 16 KB content cap with headroom; `_readAssistantFromTranscript` scans tail lines bottom-up, parses each as JSON (skip corrupt), picks the latest `type === 'assistant' && isSidechain !== true` row (sidechain = subagent transcripts that share the orchestrator JSONL), concatenates ALL `text`-type content blocks into a single string (multi-block tail markers preserved), skips tool_use-only rows. `_extractAssistantContent` now reads transcript first; original 3 fallback shapes preserved for legacy/test payloads. **Tests** (`test/m45-d2-conversation-capture.test.js` +11 cases): real-shape JSONL → body extracted, multi-block concatenation, latest-row selection, sidechain skipping, tool_use-only skipping, path-traversal /etc/passwd rejected, relative paths rejected, missing transcript_path falls through, unreadable file → stub, >1 MB transcript reads tail-only (positive control: TAIL-ONLY MARKER reachable in last 64 KB), corrupt JSON line skipped. Repurposed the prior "stub when no content" test to cover the new "stub when no content AND no transcript_path" doctrine. **Journey spec** (`e2e/journeys/conversation-content.spec.ts`): 7-frame in-session NDJSON fixture writes 3 assistant_turn frames carrying real content with unique markers (one is multi-paragraph with HEAD + TAIL markers); spec navigates to `/transcripts`, asserts `#main-stream .frame.assistant-turn` count = 3, every `.body` non-empty, each carries its expected marker, multi-paragraph TAIL marker present, USER-PROMPT marker absent from any assistant bubble, no `.frame.raw` JSON-dump fallback. **Adversarial Red Team** (`test/m53-conversation-content-redteam.test.js`, 4 tests): three broken extractor variants (A: regress-to-old-code returning null → violates I1 non-empty; B: picks user message → violates I2 marker-match; C: first-text-block-only → violates I3 tail-marker-present), each `assert.throws` with the exact invariant ID; positive control replays the real extractor and passes all three invariants on the same fixture transcript, proving the harness isn't trivially broken. **Verification**: full unit suite **2210/2210 pass** (vs 2195 baseline; +11 M45 D2 + 4 M53 redteam = +15, zero regressions); Playwright `e2e/journeys/` + `e2e/viewer/` **36/36 pass** (vs 35 baseline; +1 conversation-content). Suites that failed in the gsd-t-debug session env (`event-stream.test.js` `entry has command=null`, `watch-progress-writer.test.js` `writer_shim_safe_empty_agent_id`) are pre-existing — caused by `GSD_T_COMMAND=gsd-t-debug` / `GSD_T_PHASE=execute` env that the debug command itself sets; both pass when those vars are unset. **Note**: installed copy at `~/.claude/scripts/hooks/gsd-t-conversation-capture.js` still on the buggy version — propagates next `npm publish` + `/gsd-t-version-update-all`. Existing 6 bodyless NDJSONs in `Move-Zoom-Recordings-to-GDrive/.gsd-t/transcripts/` remain (acceptable historical records); going-forward NDJSONs will be populated. Files: `scripts/hooks/gsd-t-conversation-capture.js` (+~70 lines: 3 new fns + transcript-first dispatch), `test/m45-d2-conversation-capture.test.js` (+11 tests + repurposed stub test), `test/m53-conversation-content-redteam.test.js` (new, 4 tests), `e2e/journeys/conversation-content.spec.ts` (new), `.gsd-t/contracts/conversation-capture-contract.md` (v1.0.0 → v1.1.0 transcript-path read protocol), `CHANGELOG.md` (Unreleased section), `.gsd-t/progress.md` (this entry).

- 2026-05-06 18:32: [success] M52 COMPLETED — archived to `.gsd-t/milestones/m52-rigorous-user-journey-coverage-2026-05-06/`. Version bumped 3.22.11 → 3.23.10 (minor: feature milestone). Tagged v3.23.10. Scope: 2 file-disjoint domains, 10 tasks, 12 inaugural journey specs, 3 real-data NDJSON fixtures, hook + CLI + listener detector trio, Red Team GRUDGING PASS 5/5, suite 2195/2195 unit + 35/35 E2E. What's next: M53 candidate TBD; run `/gsd-t-status` for current project health.

- 2026-05-06 18:27: [success] M52 VERIFIED — full verification PASS across all dimensions. Unit 2195/2195, E2E 35/35 + 1 skip preserved, `gsd-t check-coverage` exit 0 (`OK: 20 listeners, 12 specs`), Red Team GRUDGING PASS (5/5 patches caught), zero shallow assertions across 12 journey specs (functional-quality audit found `not.toEqual(before)` / `toContain(TAG)` / `toBeCloseTo(pct)` patterns throughout, zero `toBeVisible/toBeAttached/toBeEnabled`-only). Goal-Backward PASS (0 placeholder patterns in M52 implementation). Doc-ripple gap closed in this pass: REQ-M52-D5-01 flipped planned → done by adding `docs/architecture.md` § "Journey Coverage Enforcement (M52)" + full M52 entry in `CHANGELOG.md` under [Unreleased]. REQ-M52-VERIFY flipped planned → done. All 13 REQ-M52 rows now done. Verification report: `.gsd-t/verify-report.md`. Auto-invoking complete-milestone next.

- 2026-05-06 18:17: [success] M52 Wave 4 — D2 T5 complete + **CHECKPOINT 3 PUBLISHED** + **M52 EXECUTED**. **Red Team category**: `templates/prompts/red-team-subagent.md` extended with new "Test Pass-Through — Journey Edition (M52)" subsection (additive — no deletions, no reorders to existing categories). Subsection prescribes: write ≥5 broken viewer patches across different journey specs; each pass-through is a verdict-level FAIL until spec is tightened; documents the hook end-to-end exercise as part of this category. **Adversarial run** (5 patches, all reverted after each test): (P1) splitter:mousedown drag handler stripped → splitter-drag spec FAILS as expected ✓; (P2) `_ssSet(SS_KEY_SPLITTER, ...)` redirected to wrong key → splitter-drag + splitter-keyboard FAIL ✓; (P3) right-rail toggle handler stubbed to early-return → right-rail-toggle FAILS ✓; (P4) M52 narrowed-guard reverted to broken M48 wide-guard `if (isInSession) return;` → click-completed-conversation FAILS ✓ (catches the M52 root-cause regression itself); (P5) auto-follow change handler localStorage write removed → auto-follow-toggle FAILS ✓. **Hook end-to-end exercise**: synthetic `fakeBtn:click` listener appended to `scripts/gsd-t-transcript.html:1617` + staged → `bash .git/hooks/pre-commit` exit 1 with structured GAP report (`GAP: scripts/gsd-t-transcript.html:1617  fakeBtn:click  (addEventListener)  no spec covers this`) ✓; manifest extended with covering entry → re-run hook exit 0 (UNBLOCKED) ✓. Both transitions logged in red-team-report.md. **Findings written**: `.gsd-t/red-team-report.md` § "M52 JOURNEY-EDITION RED TEAM" using M51 structural template (5 patches + hook section + verdict). VERDICT: **GRUDGING PASS** — 5/5 patches caught, hook gate proven, zero shallow specs found. **Final state**: 12/12 journey specs green, full E2E 35/35 + 1 skip preserved, unit 2195/2195, `gsd-t check-coverage` returns `OK: 20 listeners, 12 specs` exit 0. Production viewer code unchanged (zero net diff). **Checkpoint 3** flipped PROPOSED → PUBLISHED 2026-05-06 18:30 in `m52-integration-points.md`. REQ-M52-D3-01 + REQ-M52-D3-02 + REQ-M52-D4-02 → done. M52 is fully EXECUTED — all 3 checkpoints PUBLISHED, all REQ-M52-D{1..4} rows done, REQ-M52-VERIFY remains for `/gsd-t-verify` to consume. Files: `templates/prompts/red-team-subagent.md` (additive subsection), `.gsd-t/red-team-report.md` (append-only Journey Edition section), `.gsd-t/contracts/m52-integration-points.md` (Checkpoint 3 PUBLISHED), `docs/requirements.md` (REQ-D3-01/02 + D4-02 done).

- 2026-05-06 18:09: [success] M52 Wave 3 — D2 T2-T4 complete + **CHECKPOINT 2 PUBLISHED**. **T2** specs 1-4: `main-session-stream`, `click-completed-conversation`, `click-spawn-entry`, `splitter-drag` + initial 4-entry manifest. Each spec asserts user-visible STATE CHANGE (not element existence): main-session asserts populated #main-stream + window.__mainSessionId set; click-completed asserts bottom pane gets COMPLETED_TAG content + does NOT mirror MAIN_TAG + hash updates to completed id; click-spawn asserts bottom pane gets SPAWN_TAG content + sessionStorage/hash reflects selected id; splitter-drag asserts --main-pane-pct CSS variable shifts + sessionStorage persisted (`gsd-t.viewer.splitterPct`). **T3** specs 5-8: `splitter-keyboard` (ArrowDown shifts, Home → 20, End → 80), `right-rail-toggle` (data-collapsed flips + body data-right-rail-collapsed flips + `gsd-t.viewer.rightRailCollapsed` persisted), `completed-collapse-toggle` (data-expanded flips + `gsd-t.viewer.completedExpanded` persisted), `auto-follow-toggle` (checked state flips + `gsdt.autoFollow` localStorage persisted — NOTE: localStorage not sessionStorage for this key, per existing wireAutoFollow). **T4** specs 9-12: `kill-button` (POST /transcript/:id/kill fires + button text mutates from server response; uses sentinel PID 999999 to avoid SIGTERMing the test process — initial bug: `process.pid` killed Playwright itself), `sessionstorage-persistence` (set 3 keys → reload → keys + DOM state restored), `keyboard-shortcuts` (full Home/End/ArrowUp/ArrowDown shortcut set verified), `hashchange` (changing location.hash AFTER initial load loads bottom pane with that conversation; main pane unaffected by hash mutation — initial bug: tried `goto(/transcripts#hash)` but viewer only consumes hash on `hashchange` event, not initial load — fixed by `goto(/transcripts)` then `evaluate(() => location.hash = '#' + id)`). **Manifest**: 12 entries 1:1 with spec files; `covers[]` arrays span all 17 distinct viewer listeners (multiple listeners per spec where appropriate, e.g. splitter-drag covers mousedown + window:mousemove + window:mouseup; sessionstorage-persistence covers jumpBtn + reportBtn + stopBtn click handlers). **Coverage check**: `gsd-t check-coverage` reports `OK: 20 listeners, 12 specs` (exit 0, zero gaps, zero stale entries). **Test results**: 12/12 journey specs pass in <1s total; full E2E 35/35 (was 23/35 pre-M52 + 1 skip → +12 from M52 D2; 1 skip preserved); full unit suite 2195/2195 (no regressions). **Checkpoint 2** flipped PROPOSED → PUBLISHED 2026-05-06 18:25 in `m52-integration-points.md`. REQ-M52-D2-01 + REQ-M52-D2-02 → done. Files: `e2e/journeys/{main-session-stream, click-completed-conversation, click-spawn-entry, splitter-drag, splitter-keyboard, right-rail-toggle, completed-collapse-toggle, auto-follow-toggle, kill-button, sessionstorage-persistence, keyboard-shortcuts, hashchange}.spec.ts` (12 new), `.gsd-t/journey-manifest.json` (new, 12 entries), `.gsd-t/contracts/m52-integration-points.md` (Checkpoint 2 PUBLISHED), `docs/requirements.md` (REQ-D2-01/02 done).

- 2026-05-06 17:58: [success] M52 Wave 2 — D1 T2-T5 complete + **CHECKPOINT 1 PUBLISHED**. **T2** journey-coverage-contract.md flipped PROPOSED → STABLE, version 0.1.0 → 1.0.0. **T3** `bin/journey-coverage-cli.cjs` (107 lines) + `test/m52-d1-cli-integration.test.js` (5/5 tests pass): supports `--staged-only`, `--manifest PATH`, `--quiet`, `--project-dir`; exit codes 0 (clean) / 4 (gap or stale) / 2 (manifest missing/unreadable); discoverViewerFiles auto-detects `scripts/gsd-t-transcript.html`, `scripts/gsd-t-dashboard-server.js`, `bin/gsd-t-dashboard*.cjs`, `e2e/journeys/*.spec.ts`; vacuous-pass when zero listeners + zero specs (treats missing manifest as benign). **T4** `scripts/hooks/pre-commit-journey-coverage` (mode 0755, `set -e`, viewer-source pattern set matches contract §4) + `test/m52-d1-pre-commit-hook.test.js` (6/6 tests pass): viewer+gap → exit 1 BLOCKED, viewer+covered → exit 0, no-viewer-staged → silent pass, missing manifest → exit 1 with hint, idempotent install (≤1 BEGIN marker), round-trip (install → uninstall → install yields identical block). Hook-side install/uninstall logic mirrors what T5 wires into `bin/gsd-t.js`. **T5** `bin/gsd-t.js` wiring (46 lines additive — under 50-line budget): new constants `JOURNEY_COVERAGE_HOOK_BEGIN`/`_END` (`# >>> GSD-T journey-coverage gate >>>` / `# <<< … <<<`); new `installJourneyCoverageHook(projectDir)` (idempotent, mirrors `installPlaywrightGateHook` shape with block delimiters for clean uninstall); auto-installed in `doInit` after Playwright install when `hasUI(projectDir)`; new `case "check-coverage"` in CLI dispatch (delegates to `bin/journey-coverage-cli.cjs`); new `--install-journey-hook` flag in `doctor`. Smoke verified: `gsd-t check-coverage --help` works, `gsd-t doctor --install-journey-hook` installed the hook into this repo's `.git/hooks/pre-commit` (dogfooded — gate is now LIVE for future commits). **Checkpoint 1**: `m52-integration-points.md` flipped PROPOSED → PUBLISHED 2026-05-06 18:05. REQ-M52-D1-04 + REQ-M52-D1-05 → done. **Test suite**: 2195/2195 pass (was 2184 after Wave 1; +11 from Wave 2: 5 CLI + 6 hook). Zero regressions. Files: `bin/journey-coverage-cli.cjs` (new), `scripts/hooks/pre-commit-journey-coverage` (new), `test/m52-d1-cli-integration.test.js` (new), `test/m52-d1-pre-commit-hook.test.js` (new), `bin/gsd-t.js` (+46), `.gsd-t/contracts/journey-coverage-contract.md` (Status STABLE, version 1.0.0), `.gsd-t/contracts/m52-integration-points.md` (Checkpoint 1 PUBLISHED), `docs/requirements.md` (REQ-D1-01/02/03/04/05 + D4-01 → done).

- 2026-05-06 17:50: [success] M52 Wave 1 — D1 T1 + D2 T1 complete (file-disjoint parallel-safe pair landed sequentially in-session). **D1 T1**: `bin/journey-coverage.cjs` (308 lines) implements regex-based listener detector with single-pass string-mask precomputation (handles `//`, `/*…*/`, `<!-- -->`, `'`/`"`/template literals); exports `detectListeners`, `loadManifest`, `findGaps`, `formatReport`, `isViewerSource`, `KNOWN_KINDS`. Recognises all 6 contract §3 kinds (addEventListener, inline-handler, function-call, mutation-observer, hashchange, delegated). Verified zero parser deps — no acorn/babel/esprima imports. Smoke against real `scripts/gsd-t-transcript.html`: 17 raw addEventListener matches all detected (was 12 with naive scanner — fixed by mask-based string detection); 3 function-call entry points (`connect`, `connectMain`, `fetchMainSession`); 1 hashchange. Total 20 listeners on the live viewer. `test/m52-d1-journey-coverage.test.js` (17 tests, all pass; 15 contract cases + 2 invariants — no parser-import + sub-100ms scan). **D2 T1**: 3 NDJSON fixtures sliced from `.gsd-t/transcripts/in-session-7f70cab9-62d0-4e20-8594-e1d49064d3ff.ndjson` (220 frames) — `fixture-medium-session.ndjson` (50 frames), `fixture-completed-session.ndjson` (150 frames), `fixture-multi-spawn.ndjson` (80 frames across 3 synthesized session_ids). PII scrub applied (>200 char content truncated with `[…truncated]` marker — verified 16/55/28 marker counts). Each fixture begins with `// source:` header for traceability. `e2e/fixtures/journeys/replay-helpers.ts` exports `startReplayServer({fixture, asSessionId, inSession})` (mounts fixture into temp project dir, starts dashboard server with `port: 0` ephemeral pattern matching `e2e/viewer/dual-pane.spec.ts`), `replayFixture(page, fixtureName)` (EventSource-patch drip pattern), and `gotoAndReady`. Zero new runtime deps. Full unit suite: **2184/2184 pass** (was 2167; +17 from M52 D1 tests; zero regressions). Files: `bin/journey-coverage.cjs` (new), `test/m52-d1-journey-coverage.test.js` (new), `e2e/fixtures/journeys/{fixture-medium-session,fixture-completed-session,fixture-multi-spawn}.ndjson` (new), `e2e/fixtures/journeys/replay-helpers.ts` (new).

- 2026-05-06 17:38: [planned] M52 — task lists finalized for both domains. **D1 m52-d1-journey-coverage-tooling** (5 tasks, sequential within domain): T1 `bin/journey-coverage.cjs` listener-detector + 15-case unit test; T2 `journey-coverage-contract.md` STABLE + version 1.0.0; T3 `bin/journey-coverage-cli.cjs` + `gsd-t check-coverage` CLI surface (exit 0/4/2) + 4 integration tests; T4 `scripts/hooks/pre-commit-journey-coverage` bash hook (mode 0755, `# >>> GSD-T journey-coverage gate >>>` markers) + 6-case integration test; T5 `bin/gsd-t.js` wiring (`installJourneyCoverageHook` + `check-coverage` dispatch + `--install-journey-hook` doctor flag, < 50 lines additive) + Checkpoint 1 publication. **D2 m52-d2-journey-specs-and-fixtures** (5 tasks, sequential within domain after Checkpoint 1): T1 3 real-data NDJSON fixtures (`fixture-medium-session`, `fixture-completed-session`, `fixture-multi-spawn`) + `replay-helpers.ts` (independent of D1, parallel-safe with D1 T1); T2 specs 1–4 + manifest entries 1–4; T3 specs 5–8 + manifest 5–8; T4 specs 9–12 + manifest 12 + Checkpoint 2 publication; T5 Red Team "Test Pass-Through — Journey Edition" category + adversarial run + hook end-to-end exercise + Checkpoint 3 publication. **Waves**: Wave 1 parallel (D1 T1 ‖ D2 T1, file-disjoint); Wave 2 sequential D1 T2→T5; CHECKPOINT 1; Wave 3 sequential D2 T2→T4; CHECKPOINT 2; Wave 4 D2 T5; CHECKPOINT 3. **REQ traceability**: 12 REQ-M52-* entries in `docs/requirements.md` realigned to current domain names (`m52-d1-journey-coverage-tooling`, `m52-d2-journey-specs-and-fixtures`); REQ-M52-D1-03 updated — `.gsd-t/journey-manifest.json` (D2-authored) supersedes the originally-proposed standalone `JOURNEYS.md`; D4-02 narrowed to fixture-replay coverage of ≥3 of the 12 specs (not a separate `real-data-replay.spec.ts`). **Plan validation** via haiku subagent: PASS — REQ coverage ✓, task completeness ✓, cross-domain deps ✓, contract existence ✓, wave consistency ✓, file-disjointness ✓ (logged to `.gsd-t/token-log.md`). **Test baseline**: unit 2161/2166 (5 documented env-bleed failures, not regressions per 17:17 baseline entry). **Pre-mortem**: no historical metrics (first M52 milestone, fresh domains). Files written: D1/tasks.md, D2/tasks.md, m52-integration-points.md (added Wave Execution Groups + Execution Order), docs/requirements.md M52 traceability table.

- 2026-05-06 17:30: [success] [quick] M52 click-completed bug fix + journey spec + 3-adversary Red Team — narrowed the M48 Bug 4 guards in `scripts/gsd-t-transcript.html` from "block any in-session-* spawn id from the bottom pane" to "block only the LIVE main session id (in-session-{__mainSessionId})", restoring click-to-load for ALL completed in-session conversations in the COMPLETED rail. Source changes: (1) `connectMain(sessionId)` exposes `window.__mainSessionId`; (2) renderRailEntry click handler ~line 1011 narrowed to `if (isInSession && node.spawnId === ('in-session-' + window.__mainSessionId)) return;`; (3) hashchange handler ~line 1303 narrowed to `if (id && id === ('in-session-' + window.__mainSessionId)) { return; }`; (4) legacy renderTree click handler ~line 880 same narrowed pattern. Removed the unconditional in-session-* scrub from initial-bottom-id seeding (~line 1310); added a `fetchMainSession` callback that clears the bottom-pane seed when it collides with the live main session id (preserves M48 Bug 4 mirror prevention). New journey spec `e2e/viewer/click-completed.spec.ts` (4 tests). Adversarial Red Team applied 3 broken patches per the M51 pattern — all 3 caught (see `.gsd-t/red-team-report.md` § M52 RED TEAM FINDINGS). Updated 5 unit assertions in `test/m48-viewer-rendering-fixes.test.js` to assert the M52 narrowed pattern; added 1 new unit test for the fetchMainSession seed-collision callback. Final state: E2E 23/23 + 1 placeholder skip; unit 2167/2167 pass. Files: `scripts/gsd-t-transcript.html`, `e2e/viewer/click-completed.spec.ts` (new), `test/m48-viewer-rendering-fixes.test.js`, `.gsd-t/red-team-report.md`.

- 2026-05-06 17:29: [partitioned] M52 — 2 file-disjoint domains. **D1 m52-d1-journey-coverage-tooling** owns `bin/journey-coverage.cjs` + `bin/journey-coverage-cli.cjs` + `scripts/hooks/pre-commit-journey-coverage` + `journey-coverage-contract.md` + 3 D1 test files; additive ~50-line edits in `bin/gsd-t.js` (`installJourneyCoverageHook`, `--install-journey-hook` doctor flag, install-flow autoinstall). **D2 m52-d2-journey-specs-and-fixtures** owns `e2e/journeys/` (12 specs) + `e2e/fixtures/journeys/` (3 NDJSON + replay-helpers.ts) + `.gsd-t/journey-manifest.json`; additive subsection in `templates/prompts/red-team-subagent.md`. **Disjointness**: only shared file is `bin/gsd-t.js` (D1 only). **Contracts**: `journey-coverage-contract.md` v0.1.0 (PROPOSED → STABLE on D1 task-5) defines manifest schema + 6 listener kinds (addEventListener, inline-handler, function-call, mutation-observer, hashchange, delegated) + gap rules + exit codes (0=clean, 4=gaps, 2=manifest-missing) + idempotent hook installer mirroring M50. `m52-integration-points.md` defines 3 checkpoints: C1 D1-publishes-CLI-and-hook (blocks D2 manifest), C2 D2-publishes-12-specs-and-fixtures (blocks verify), C3 Red-Team-adversarial-pass (blocks complete-milestone). **Assumption audit**: M50 references locked INSPECT (read pattern, parallel build); D1 must read `installPlaywrightSync` + `pre-commit-playwright-gate` + `installPlaywrightGateHook` before authoring parallel hook; D2 must read `e2e/viewer/dual-pane.spec.ts` + `e2e/viewer/click-completed.spec.ts` + transcript.html lines 353-1614 before authoring journey specs. "Covered" semantics locked: spec must assert on user-visible state changed by listener (functional, per CLAUDE.md § E2E Test Quality Standard) — not mere existence/firing. **Surfaces**: single CLI consumer (no SharedCore needed). **Scan freshness**: scan 199 commits / 20 days stale, NOT auto-refreshed — partition is a test-rigor + tooling milestone with explicit milestone-defined scope; scan data is not load-bearing for these decisions. Refresh deferred to next architecture-touching milestone.
- 2026-05-06 17:17: [baseline] M52 pre-partition test baseline — `npm test` reports tests 2166, pass 2161, fail 5. The 5 failures are the documented M51 env-bleed pattern: when M52 milestone command runs in-session, `GSD_T_AGENT_ID/COMMAND/PHASE/PROJECT_DIR/PARENT_AGENT_ID` env vars are set by the harness and leak into `tests/event-stream.test.js` + `tests/watch-progress-writer.test.js` (which assert on shell-form filenames + null parent). Reproduces cleanly: failures vanish when run with the env vars unset. Not a regression. M52 inherits this — tracked under techdebt rather than blocking partition. Suite is otherwise clean.
- 2026-05-06 17:17: [defined] M52 — Rigorous User-Journey Coverage + Anti-Drift Test Quality. Rationale: M48→M49→M50→M51 drift pattern — each test round catches the bug shape the previous round named, never the unnamed shape (e.g., M48 missed click-on-completed-entry; M51 strengthened existing assertions but added zero new surfaces). Architectural fix has two parts: (a) MECHANICAL coverage via `bin/journey-coverage.cjs` AST/text walker + pre-commit-journey-coverage hook that blocks commits when viewer-source files staged AND uncovered listeners exist; (b) DOCTRINAL change to what "rigorous" means — journey specs walk every interactive surface end-to-end with assertions on user-visible state, real-data NDJSON fixtures, adversarial Red Team scoped to JOURNEYS not lines. Scope: D1 journey-coverage tooling + pre-commit hook + installer wiring; D2 12 inaugural journey specs (`main-session-stream`, `click-completed-conversation`, `click-spawn-entry`, `splitter-drag`, `splitter-keyboard`, `right-rail-toggle`, `completed-collapse-toggle`, `auto-follow-toggle`, `kill-button`, `sessionstorage-persistence`, `keyboard-shortcuts`, `hashchange`); D3 Red Team "Test Pass-Through — Journey Edition" mode + adversarial run; D4 real-data fixture replay (3 captured NDJSONs); D5 5-file doc-ripple (CLAUDE-global + templates + 4 command files + architecture). Non-goals: auto-generating spec bodies, touching unit tests, modifying M50 spawn-time gate. Success: zero coverage gaps, 12 specs green, deliberate-handler-without-spec commit blocked, 5+ Red Team breakages all caught, real-data replays pass, 2166 + ~12 + 3 suites all green.
- 2026-05-06 15:36: [success] [debug] M51 viewer-spec rigor — fixed TEST-M50-001 (dual-pane false positive) + strengthened all 5 viewer Playwright specs to outcome-based assertions + ran adversarial Red Team that wrote 5 broken viewer impls and proved every strengthened spec catches its targeted regression. Specs grew from 9 tests → 19 tests; full E2E suite 19/19 pass, full unit suite 2166/2166 pass (env vars `GSD_T_AGENT_ID/COMMAND/PHASE/PROJECT_DIR/PARENT_AGENT_ID` must be unset for clean run — they bleed env state into tests/event-stream.test.js + tests/watch-progress-writer.test.js when run inside an active gsd-t-debug session). Reproduction: 5 adversary patches in `.gsd-t/red-team-report.md` § "M51 RED TEAM FINDINGS". Production viewer code unchanged (no real bugs found behind the adversary patches; this was test-suite rigor work). Files modified: `e2e/viewer/{title,timestamps,chat-bubbles,dual-pane,lazy-dashboard}.spec.ts`, `.gsd-t/red-team-report.md`. Pre-existing port-collision issue in fixtures (`Math.floor(Math.random() * 100)` ports) replaced with ephemeral `port: 0` + `server.address().port` readback.
- 2026-05-06 14:49: [success] Milestone "M50 — Universal Playwright Bootstrap + Deterministic UI Enforcement" completed — archived to `.gsd-t/milestones/m50-universal-playwright-bootstrap-2026-05-06/`, version bumped 3.21.12 → **3.22.10** (minor: feature milestone). Tagged v3.22.10. 2 domains file-disjoint, 15 tasks, single-day in-session build. Three executable enforcement layers: (1) bootstrap library wired into init/update-all/doctor/setup-playwright; (2) spawn-gate in headless-auto-spawn.cjs; (3) pre-commit-playwright-gate hook. 5 E2E viewer specs + playwright.config.ts. 62 new unit tests; E2E 8/9 (TEST-M50-001 deferred). Goal-Backward PASS, zero regressions.
- 2026-05-06 14:15: [success] M50 D1 task-3 — installPlaywright() landed on bin/playwright-bootstrap.cjs. Idempotent installer per contract §3+§6+§7+§8. Added 9 install-path tests to test/m50-d1-playwright-bootstrap.test.js (idempotent short-circuit, 4 package-manager paths, config-template verbatim write, existing-config preservation, existing-e2e/ preservation, install-failure → hint, chromium-failure → partial:true). 20/20 in this file pass.
- 2026-05-06 14:15: [success] M50 D1 task-4 — bin/gsd-t.js wired. Inline hasPlaywright (lines 201-204) replaced with require('./playwright-bootstrap.cjs'); init flow auto-installs Playwright when hasUI && !hasPlaywright; checkProjectHealth made async + auto-installs across registered projects + reports counts in update-all summary; checkDoctorProject made async + supports --install-playwright flag; new `gsd-t setup-playwright [path]` subcommand wraps installPlaywright with --force escape hatch for non-UI projects. New test/m50-d1-cli-integration.test.js (5/5 pass).
- 2026-05-06 14:15: [checkpoint] M50 D1 published — exports verified, 43/43 D1 tests passing (18 ui-detection + 20 playwright-bootstrap + 5 cli-integration), `gsd-t doctor --install-playwright` flag recognized, `gsd-t setup-playwright` subcommand functional. m50-integration-points.md flipped to PUBLISHED. D2 may now import from bin/playwright-bootstrap.cjs and bin/ui-detection.cjs.
- 2026-05-06 14:25: [success] M50 D2 task-1 — playwright.config.ts at GSD-T project root (testDir './e2e', chromium project, webServer omitted), e2e/__placeholder.spec.ts, package.json scripts ('e2e', 'e2e:install') + @playwright/test in devDependencies (zero runtime-deps preserved). 4/4 in test/m50-d2-viewer-specs-smoke.test.js.
- 2026-05-06 14:25: [success] M50 D2 task-2 — spawn-gate landed in bin/headless-auto-spawn.cjs::autoSpawnHeadless. New TESTING_OR_UI_COMMANDS Set (9 commands). Gate fires when isTestingOrUICommand && hasUI && !hasPlaywright; calls installPlaywrightSync (added to bin/playwright-bootstrap.cjs alongside async installPlaywright); on install failure writes mode:'blocked-needs-human' + reason + err + hint to the headless session-state file and process.exit(4). Test injection points: _gateInstaller, _gateProbes, _gateExit. 9/9 in test/m50-d2-spawn-gate.test.js. 101/101 in pre-existing headless test files (no regressions).
- 2026-05-06 14:25: [success] M50 D2 task-3 — scripts/hooks/pre-commit-playwright-gate (executable bash, mode 0755) reads .gsd-t/.last-playwright-pass and blocks viewer-source commits when staged-file mtime > the timestamp. Patterns: scripts/gsd-t-transcript.html, scripts/gsd-t-dashboard-server.js, e2e/viewer/. Fail-open on missing/corrupt timestamps. New `gsd-t doctor --install-hooks` flag (idempotent install via PLAYWRIGHT_GATE_HOOK_MARKER pattern, mirrors capture-lint hook approach). 6/6 in test/m50-d2-pre-commit-hook.test.js.
- 2026-05-06 14:25: [success] M50 D2 tasks 4-8 — 5 viewer E2E specs landed in e2e/viewer/: title.spec.ts (M48 Bug 1: project basename in <title>/.title for /transcripts and /transcripts/{spawn-id} + $&/$1 backref defense), timestamps.spec.ts (M48 Bug 2: distinct ts → distinct rendered HH:MM:SS), chat-bubbles.spec.ts (M48 Bug 3: user_turn/assistant_turn/session_start/tool_use_line render as bubbles, not JSON.stringify dumps), dual-pane.spec.ts (M48 Bug 4: bottom pane never connects to in-session-* SSE; uses EventSource constructor patch), lazy-dashboard.spec.ts (M49 banner shape: file path + visualize hint vs Live transcript URL).
- 2026-05-06 14:25: [success] M50 D2 task-9 — doc-ripple complete in single pass: ~/.claude/CLAUDE.md + templates/CLAUDE-global.md (Playwright Readiness Guard collapsed to layered referral), commands/gsd-t-init.md Step 11 (points at installPlaywright()), docs/architecture.md (new "Playwright Deterministic Enforcement (M50)" subsection), CHANGELOG.md (full M50 entry under [Unreleased]).
- 2026-05-06 14:25: [success] M50 D2 task-10 — playwright-bootstrap-contract.md Status flipped PROPOSED→STABLE. 16 REQ-M50-D1/D2 traceability rows flipped from 'planned' to 'done' in docs/requirements.md. REQ-M50-VERIFY stays 'planned' (verified by /gsd-t-verify, not by execute).
- 2026-05-06 14:25: [milestone-executed] M50 EXECUTED — both domains complete (15/15 tasks). 62 new M50 tests pass cleanly. Zero regressions. Verified via gsd-t-verify gate.
- 2026-05-06 14:42: [success] M50 VERIFIED-WITH-WARNINGS — Goal-Backward PASS (16 REQs, 0 placeholders). 2 CONDITIONAL items (test-quality, not production). 1 WARN (doc-ripple partial — substantive migration complete). Auto-invoked complete-milestone.
- 2026-05-06 13:34: [defined] M50 — rationale: M48 viewer fixes shipped without Playwright tests despite prose rule; 14/19 registered projects Playwright-missing. Convert prose to executable gates: bootstrap library, spawn-gate, commit-time hook. Also land 5 E2E viewer specs.
- 2026-05-06 13:45: [partitioned] M50 — 2 file-disjoint domains. D1 bootstrap-and-detection (5 tasks); D2 gates-and-specs (10 tasks). Cross-domain checkpoint after D1 lands (both touch bin/gsd-t.js).
