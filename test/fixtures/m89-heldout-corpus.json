{
  "_comment": "M89 D1 HELD-OUT generalization corpus (finding #1, the CRITICAL anti-self-fulfilling-oracle guard; refreshed for the premise-correction v1.2.0). Input is a GUESSED CLAIM, not 'a gap'. These claims are NOT used to author/tune the classifier — they are NOVEL claims the classifier must label correctly by FEATURE CLASS (does the claim reference a third-party/library/platform/spec proper-noun OR assert an external system's behavior/return-shape/limit without a source, vs. a this-repo path/symbol/file), NOT by hard-coded keywords scraped from the seen 13-item corpus. CRITICAL premise-correction items: HO-E4 is a PROPER-NOUN-LESS external claim (asserts an external endpoint's max-batch-size with no vendor name — must still route external because it ASSERTS an external system's behavior unverified) and HO-I4 is a SYMBOL-ONLY internal claim (bare local symbol, no path/anchor). A classifier that passes test/fixtures/m89-labeled-corpus.json 13/13 but mislabels ANY held-out item FAILS A1 (proves it memorized keywords instead of generalizing). None of these proper nouns (Stripe, Chrome storage-quota, CSS :has()) or repo symbols (isOrderLocked, gsd-t-verify.workflow.js, cli-preflight, resolveProfile) appears in the seen corpus.",
  "items": [
    {
      "id": "HO-E1",
      "source": "held-out (novel external)",
      "gap": "What is the exact name of the Stripe webhook signature-verification header, and how is the signed payload constructed?",
      "expectedClass": "external",
      "expectedRoute": "web",
      "featureSignal": "third-party API (Stripe) + spec/HTTP-header term — proper-noun + capability, not in this repo"
    },
    {
      "id": "HO-E2",
      "source": "held-out (novel external)",
      "gap": "What is the per-origin storage-quota limit for the Chrome extension storage.local API?",
      "expectedClass": "external",
      "expectedRoute": "web",
      "featureSignal": "platform/browser runtime behavior (Chrome) + limit term"
    },
    {
      "id": "HO-E3",
      "source": "held-out (novel external)",
      "gap": "Which browsers currently support the CSS :has() selector and from what version?",
      "expectedClass": "external",
      "expectedRoute": "web",
      "featureSignal": "published standard/spec (CSS) + browser-support/version facts"
    },
    {
      "id": "HO-E4",
      "source": "held-out (novel external — PROPER-NOUN-LESS, the premise-correction guard)",
      "gap": "The payments endpoint accepts a maximum batch size of 100 items per request.",
      "expectedClass": "external",
      "expectedRoute": "web",
      "featureSignal": "PROPER-NOUN-LESS external assertion: asserts an external system's (a remote payments endpoint, outside this repo) behavior/limit with NO vendor proper noun and NO cited source — an unverified external claim → external. A keyword router keyed on vendor names would wrongly default this internal (the silent-miss cycle-2 finding #3)."
    },
    {
      "id": "HO-I1",
      "source": "held-out (novel internal)",
      "gap": "What does isOrderLocked return in this repo when the order is already locked?",
      "expectedClass": "internal",
      "expectedRoute": "grep",
      "featureSignal": "this-repo local symbol (isOrderLocked) + 'this repo' anchor — grep-able"
    },
    {
      "id": "HO-I2",
      "source": "held-out (novel internal)",
      "gap": "Which domain owns templates/workflows/gsd-t-verify.workflow.js?",
      "expectedClass": "internal",
      "expectedRoute": "grep",
      "featureSignal": "repo-relative path (gsd-t-verify.workflow.js) — file-ownership question"
    },
    {
      "id": "HO-I3",
      "source": "held-out (novel internal)",
      "gap": "What exit code does cli-preflight use when it hard-fails on a wrong branch?",
      "expectedClass": "internal",
      "expectedRoute": "grep",
      "featureSignal": "known local symbol/file (cli-preflight) — repo's own code"
    },
    {
      "id": "HO-I4",
      "source": "held-out (novel internal — SYMBOL-ONLY, no path/anchor)",
      "gap": "Does resolveProfile clamp the competition-judge model when it equals the producers' model?",
      "expectedClass": "internal",
      "expectedRoute": "grep",
      "featureSignal": "SYMBOL-ONLY internal: a bare local symbol (resolveProfile) with NO repo-relative path and NO 'this repo' anchor — still internal because the symbol is grep-able local code, not an external system. Pairs with HO-E4 (proper-noun-less external) to prove the classifier decides by feature class (local-symbol vs external-assertion), not by surface keywords."
    }
  ]
}
