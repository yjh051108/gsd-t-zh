# {标题} — {此行为为映射的一句话主题}

**{该主题}行为的伪代码 + 意图，端到端。**
涵盖系统在每个决策点的位置、何时拒绝，以及结果如何到达每个消费者。
**{范围说明 — 这是关于什么的，以及它刻意不做的一件事。}**

> **⚠ {影响全局的指令}（{作者}, {日期}）。** {读者必须注意的一句话 — 是整个映射背后的 WHY，以用户的意图表达。此处的文字是 USER 的指令，绝不可是 agent 的推理。}

_面向 **{里程碑 / 范围 ID}** 的前瞻行为映射。尚未构建 —
扎根于它必须尊重的**现有合同**：
`{现有合同-a}.md`、`{现有合同-b}.md`，以及冻结的
`{Schema}`（`{字段}` / `{字段}` / `{字段}`）。 companion to
`PseudoCode-{Other}.md`。参见 `{roadmap ref}` 了解里程碑范围。_

<!--
  ─────────────────────────────────────────────────────────────────────────────
  如何使用此模板  （在实际实例中删除此注释块）
  ─────────────────────────────────────────────────────────────────────────────
  • 实例命名为 PseudoCode-[Title].md，其中 [Title] 是主题
    （例如 PseudoCode-PayPal.md），不要用里程碑 ID。只有这个空白模板保留
    `-spec` 后缀。
  • 按顺序编写两个高度：
      1. 高级方法  — 做什么 / 为什么 / 何时，参与者，一句话总结表。
         不要写字段级细节。**先签署此部分**。
      2. 详细  — 完整的编号 `##` 章节集（见下方示例的颗粒度）。
         每个决策边界一节。
  • 每个章节携带**五个章节元素**（见合同 §1）：
      意图文字 · 机制伪代码 · 一句话总结 · [RULE] 守卫图 · ⚠ 分歧标志 · 附录。
  • 每个 `> **Intention.**` 文字块位于其围栏伪代码**上方**，带日期 + 署名；
    文字是 USER 的 WHY，绝不可是 agent 推理。
  • 机制块扎根于**现有合同 / schema**，具体标识符推迟到基于真实 schema 的计划阶段。
  • 守卫图：将每一个不变量渲染为一行 `[RULE] <不变量>`（或标记的 `<不变量> [RULE — <标签>]` 形式）。
    一个标记 = 一条规则。
  • 分歧：凡 NEW 意图取代已交付代码的地方，写显式的
    `⚠ Divergence:` 标志。保持不变 = 删除此标志。
  • 用以下方式引用每个实施计划任务：
        **PseudoCode-Section**: {标题}#<github-slug-of-the-## heading>
  语法归合同所有：
  `.gsd-t/contracts/pseudocode-source-of-truth-contract.md`
  （§2 守卫图，§3 章节引用，§4 分歧）。不要在此处重新推导。
  ─────────────────────────────────────────────────────────────────────────────
-->

---

<!-- ═══════════════ 高度 1 — 高级方法（先签署） ═══════════════ -->

## 一个 {调用 / 决策}，一句话

> **高级方法（{作者}, {日期}）。** {此处的做法、原因和时机 —
> 参与者和单一决策，用简洁的语言表达。不要写字段级细节。
> 此高度在下方详细章节编写前签署完毕。}

| {调用 / 决策} | 位于 | 决定 | 仅在满足以下条件时运行… |
|-------------------|----------|---------|-----------------|
| **{调用}** | **{领域 — Server / Extension / …}** | {它将输入变成什么} | {触发它的前提条件} |
| **{读取 / 回退}** | **{领域}** | {它回答的问题} | {卖家 / 事件触发的触发器} |

> **范围（{作者}, {日期}）。** {故意的越出范围边界 —
> 这个映射**不做**什么。说明它接触的一个状态 / 调用 / 表面，
> 以及它拒绝接触的。这使详细章节保持诚实。}

---

<!-- ═══════════════ 高度 2 — 详细（示例颗粒度） ═══════════════ -->

## 0. 此处从何处开始 — {前提条件 / 构建前存在什么}

> **意图（{作者}, {日期}）。** {此映射开始时的系统状态 —
> 前提条件。如果这取代了已交付模型，在此说明并在下方添加
> ⚠ 分歧标志。}

```text
PRECONDITION:
  {确切初始状态 — 已加载 / 已分组 / 已存在什么，以及
   刻意尚未持久化的内容。}
```

> ⚠ **与已交付的 {什么} 分歧（计划时协调）：** {已交付代码今天的做法} vs.
> {此意图的做法}。{什么变成死代码 / 什么被复用。}
> 标记为 {里程碑} 计划使用。
> （保持 = 删除此标志。取代 = 保留，依据合同 §4。）

---

## 1. {第一个边界} — {触发器 → 拥有它的参与者}

> **意图。** {此步骤存在的原因以及用户希望它做什么 —
> 指令。一段简短的文字，用户的声音。}

```text
{参与者}  在 {触发器} 上:
    {步骤}
    {调用 / 守卫}
    在 {结果}:  {发生什么}
    在 {失败}:  {安全回退 — 绝不可崩溃}
```

---

## 2. {第二个边界} — `{操作}` （★ {为什么这个是影响全局的}）

> **意图。** {原因。说出此章节要保护的唯一不变量 —
> 例如 "每个使双击无害的守卫"。}

```text
{operationName}({输入}):

    # ── 门 1 — {它验证什么}（RULE）───────────────────────────────
    # 意图: {此门存在的原因，用户的声音}。
    {加载 / 锁定}
    如果 {前提条件失败}:   → {状态 / 错误}   # {它保护什么}

    # ── 门 2 — {后备}（RULE, 后备）──────────────────────────────
    # 意图: {原因 — 如果上游已阻止则称为后备}。
    如果 {不可能的条件}:   → {状态}

    # ── 步骤 3 — {副作用的调用} ─────────────────────  ★ {标记}
    # 意图: {什么跨越了边界，什么推迟到真实 schema}。
    {调用}                                   # 具体 ID 推迟到计划时间

    # ── 步骤 4 — 持久化 + {原子效应}（RULE, 一次 tx）─────────────────
    # 意图: {记录诞生 / 状态翻转 — 原子性地}。
    在一个 tx 内:
        {写入}
        {翻转状态}
    返回 {成功形状}

    # ── 失败 — 绝不完全{执行该操作}（RULE）────────────────────────
    # 意图: 失败的 {操作} 不持久化任何内容 — 安全重试。
    在 {失败} (任何点):  不持久化任何内容 ; → {状态}
```

---

## 3. {更多边界按需} — {每个决策一个 `##` 章节}

> **意图。** {添加任意数量的编号 `##` 章节 —
> 匹配示例的颗粒度，每个真实决策一节，不要一个大章节。}

```text
{此边界的机制}
```

---

## {N}. {主题}安全图 — 每个守卫，一行 [RULE]

```text
门: {条件} → {状态}                     [RULE] {一行的不变量}
门: {锁定 / 序列化}                          [RULE] {它防止什么}
{确定性守卫}                             [RULE — {标签}] {不变量，标记形式}
{永不执行此操作}                                   [RULE] {禁止}
{在此确切点诞生 / 设置}                  [RULE] {生命周期不变量}
在 {失败}: 持久化 NOTHING                     [RULE] {安全失败不变量}
```

**{一段文字重述整个安全故事：记录何时诞生，锁意味着什么，
绝对不可能发生的一件事，以及为什么每个重试 / 双重操作都是无害的。}**

---

## {可选} — {一个存储 / 共享状态 / 已知差距备注}

> {可选章节，示例携带的内容：共享存储备注、"已知差距 /
> 状态（截至 {版本}）"列表。仅当主题具有这些时包含。}

---

## 附录 — 原始伪代码（不含意图注释）

```text
# ════════════════════════════════════════════════════════════════════════════
# {领域 A} — {它覆盖的内容}
# ════════════════════════════════════════════════════════════════════════════
{§1–§N 机制，意图文字已剥离 — 构建的快速参考}

# ════════════════════════════════════════════════════════════════════════════
# {领域 B} — {它覆盖的内容}
# ════════════════════════════════════════════════════════════════════════════
{operationName}({输入}):
    {守卫}                                       # {一行原因}
    {调用}                                    # ★ {影响全局的调用}
    tx: {写入} ; {翻转状态}
    返回 {成功}
    在失败时: 持久化 NOTHING ; → {状态}         # 安全重试
```
